---
tags: [Plector, 规范, 设计]
type: spec
created: 2026-04-08
---

# Plector Harness 完善（Claude Code 可执行版）

> 本文档为增量更新，Plector 核心模块和业内标准对齐已完成。
> 本次更新：完善 Harness，约束 Claude Code 的代码输出质量。
>
> 新增检查项：
> - 依赖方向检查（core/ 不依赖 skills/ tools/）
> - 代码格式检查（ruff）
> - 函数长度检查（≤50 行）
> - 类型检查（mypy）
>
> 共 7 步，按顺序执行。

---

## 第一步：安装依赖

```bash
pip install ruff mypy
```

更新 `requirements.txt`：

```txt
psutil>=5.9.0
pyyaml>=6.0
pytest>=7.0
pytest-asyncio>=0.21
pre-commit>=3.0
ollama>=0.1.0
openai>=1.0.0
anthropic>=0.20.0
ruff>=0.4.0
mypy>=1.10.0
```

### 验证

```bash
ruff --version
mypy --version
```

### 提交

```bash
git add requirements.txt
git commit -m "chore: 添加 ruff 和 mypy 依赖"
```

---

## 第二步：创建依赖方向检查 `scripts/check_dependencies.py`

```python
#!/usr/bin/env python3
"""
检查依赖方向是否违规

规则：
  core/     → 不依赖 skills/、tools/
  skills/   → 可依赖 core/，不依赖其他 skills/
  tools/    → 不依赖 skills/、core/

用法：
  python scripts/check_dependencies.py
  exit code 0 = 通过，非 0 = 有违规
"""
import ast
import sys
from pathlib import Path

# 依赖方向规则
RULES = {
    "core": {"forbidden": ["skills", "tools"]},
    "skills": {"forbidden": ["skills"]},  # skills 不依赖其他 skills
    "tools": {"forbidden": ["skills", "core"]},
}


def get_imports(filepath: Path) -> list:
    """从 Python 文件中提取顶层 import"""
    imports = []
    try:
        with open(filepath, encoding="utf-8") as f:
            tree = ast.parse(f.read())
    except SyntaxError:
        return imports
    for node in ast.walk(tree):
        if isinstance(node, ast.Import):
            for alias in node.names:
                imports.append(alias.name.split(".")[0])
        elif isinstance(node, ast.ImportFrom):
            if node.module:
                imports.append(node.module.split(".")[0])
    return imports


def check_file(filepath: Path, module: str) -> list:
    """检查单个文件的依赖是否违规"""
    errors = []
    imports = get_imports(filepath)
    forbidden = RULES.get(module, {}).get("forbidden", [])
    for imp in imports:
        if imp in forbidden:
            errors.append(f"{filepath}: 不允许导入 {imp}/（{module}/ 不应依赖 {imp}/）")
    return errors


def main():
    errors = 0
    for module in ["core", "skills", "tools"]:
        module_dir = Path(module)
        if not module_dir.exists():
            continue
        for py_file in module_dir.rglob("*.py"):
            if py_file.name == "__init__.py":
                continue
            file_errors = check_file(py_file, module)
            for err in file_errors:
                print(f"❌ {err}")
            errors += len(file_errors)

    if errors == 0:
        print("✅ 依赖方向检查通过")
    sys.exit(errors)


if __name__ == "__main__":
    main()
```

### 验证

```bash
python scripts/check_dependencies.py
# 应输出: ✅ 依赖方向检查通过
```

### 提交

```bash
git add scripts/check_dependencies.py
git commit -m "feat(scripts): 添加依赖方向检查脚本"
```

---

## 第三步：创建函数长度检查 `scripts/check_function_length.py`

```python
#!/usr/bin/env python3
"""
检查函数长度是否超过限制

规则：
  单个函数不超过 50 行

用法：
  python scripts/check_function_length.py
  exit code 0 = 通过，非 0 = 有违规
"""
import ast
import sys
from pathlib import Path

MAX_LINES = 50


def count_function_lines(node: ast.FunctionDef, source_lines: list) -> int:
    """计算函数的实际行数"""
    start = node.lineno
    end = node.end_lineno if hasattr(node, "end_lineno") else start
    return end - start + 1


def check_file(filepath: Path) -> list:
    """检查单个文件中的函数长度"""
    errors = []
    try:
        with open(filepath, encoding="utf-8") as f:
            source = f.read()
            source_lines = source.splitlines()
            tree = ast.parse(source)
    except SyntaxError:
        return errors

    for node in ast.walk(tree):
        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):
            length = count_function_lines(node, source_lines)
            if length > MAX_LINES:
                errors.append(
                    f"{filepath}:{node.lineno} {node.name}() 长度 {length} 行，超过 {MAX_LINES} 行限制"
                )
    return errors


def main():
    errors = 0
    for py_file in Path(".").rglob("*.py"):
        # 跳过第三方和虚拟环境
        if any(skip in str(py_file) for skip in ["venv", ".venv", "__pycache__", "node_modules"]):
            continue
        file_errors = check_file(py_file)
        for err in file_errors:
            print(f"❌ {err}")
        errors += len(file_errors)

    if errors == 0:
        print("✅ 函数长度检查通过")
    sys.exit(errors)


if __name__ == "__main__":
    main()
```

### 验证

```bash
python scripts/check_function_length.py
# 应输出: ✅ 函数长度检查通过
```

### 提交

```bash
git add scripts/check_function_length.py
git commit -m "feat(scripts): 添加函数长度检查脚本（≤50 行）"
```

---

## 第四步：创建 ruff 配置 `pyproject.toml`

```toml
[project]
name = "plector"
version = "1.0.0"

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort（导入排序）
    "N",    # pep8-naming（命名规范）
    "UP",   # pyupgrade（Python 版本升级建议）
    "B",    # flake8-bugbear（常见 bug）
    "SIM",  # flake8-simplify（代码简化）
    "RUF",  # ruff 自有规则
]
ignore = [
    "E501",   # 行长度（由 line-length 控制）
    "N812",   # 允许 import lowercase as Uppercase
    "SIM108", # 允许三元表达式替代 if/else
]

[tool.ruff.lint.isort]
known-first-party = ["core", "skills", "tools", "channels"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
```

### 验证

```bash
ruff check core/ skills/ tools/ channels/
# 应无错误（或只有可忽略的警告）
```

### 提交

```bash
git add pyproject.toml
git commit -m "chore: 添加 ruff + mypy 配置（pyproject.toml）"
```

---

## 第五步：更新 pre-commit 配置

替换 `.pre-commit-config.yaml`：

```yaml
repos:
  # 1. 依赖方向检查
  - repo: local
    hooks:
      - id: check-dependencies
        name: Check dependency direction
        entry: python scripts/check_dependencies.py
        language: system
        files: ^(core|skills|tools)/.*\.py$

  # 2. 函数长度检查
  - repo: local
    hooks:
      - id: check-function-length
        name: Check function length (≤50 lines)
        entry: python scripts/check_function_length.py
        language: system
        files: \.py$

  # 3. 技能语法检查
  - repo: local
    hooks:
      - id: check-skill-syntax
        name: Check skill Python syntax
        entry: python scripts/check_skills.py
        language: system
        files: ^skills/.*\.py$

  # 4. skill.json 格式检查
  - repo: local
    hooks:
      - id: validate-skill-json
        name: Validate skill.json (MCP format)
        entry: python scripts/validate_skills.py
        language: system
        files: ^skills/.*/skill\.json$

  # 5. ruff 代码格式检查
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.4
    hooks:
      - id: ruff
        args: [--fix]
        files: ^(core|skills|tools|channels)/
      - id: ruff-format
        files: ^(core|skills|tools|channels)/
```

### 验证

```bash
pre-commit install
pre-commit run --all-files
```

### 提交

```bash
git add .pre-commit-config.yaml
git commit -m "chore: 完善 pre-commit 配置（依赖方向 + 函数长度 + ruff）"
```

---

## 第六步：更新 CLAUDE.md

在 `## 验证命令` 中添加：

```markdown
## 验证命令

```bash
# 语法检查
python -m py_compile core/agent_loop.py

# 依赖方向
python scripts/check_dependencies.py

# 函数长度
python scripts/check_function_length.py

# 技能校验
python scripts/validate_skills.py

# 代码格式
ruff check core/ skills/ tools/ channels/

# 单元测试
pytest tests/ -v

# 全部检查（pre-commit）
pre-commit run --all-files
```

在 `## 提交规范` 中更新检查清单：

```markdown
## 提交前检查清单

- [ ] `python -m py_compile <file>.py` 无语法错误
- [ ] `python scripts/check_dependencies.py` 依赖方向正确
- [ ] `python scripts/check_function_length.py` 函数 ≤50 行
- [ ] `python scripts/validate_skills.py` skill.json 格式正确
- [ ] `ruff check` 无错误
- [ ] 无 `print()` 调试语句
- [ ] 所有异常都有处理
- [ ] 阻塞调用用 `run_in_executor`

提交格式：
```
<type>(<scope>): <subject>
```
- type: feat / fix / docs / refactor / test / chore
- 提交后：`git push`
```

### 提交

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md 添加 Harness 检查命令和提交清单"
```

---

## 第七步：端到端验证

```bash
# 1. 依赖方向
python scripts/check_dependencies.py
# 预期: ✅ 依赖方向检查通过

# 2. 函数长度
python scripts/check_function_length.py
# 预期: ✅ 函数长度检查通过

# 3. 技能语法
python scripts/check_skills.py
# 预期: exit code 0

# 4. skill.json 格式
python scripts/validate_skills.py
# 预期: exit code 0

# 5. ruff 代码格式
ruff check core/ skills/ tools/ channels/
# 预期: 无错误

# 6. mypy 类型检查（可选，可能有警告）
mypy core/ --ignore-missing-imports
# 预期: 无 error（warning 可忽略）

# 7. 单元测试
pytest tests/ -v
# 预期: 全部通过

# 8. pre-commit 全部检查
pre-commit run --all-files
# 预期: 全部 passed
```

### 最终提交

```bash
git add -A
git commit -m "feat: 完善 Harness（依赖方向 + 函数长度 + ruff + pre-commit）"
git push
```

---

## 改动汇总

| 文件 | 操作 | 检查什么 |
|------|------|---------|
| `scripts/check_dependencies.py` | 新建 | core/ 不依赖 skills/ tools/ |
| `scripts/check_function_length.py` | 新建 | 函数 ≤50 行 |
| `pyproject.toml` | 新建 | ruff + mypy 配置 |
| `.pre-commit-config.yaml` | 修改 | 添加 3 个新检查 |
| `requirements.txt` | 修改 | 添加 ruff + mypy |
| `CLAUDE.md` | 修改 | 更新验证命令和提交清单 |

## 检查流程

```
Claude Code 修改文件
  ↓ git commit
  ↓ pre-commit 触发
  ↓
  ├── 1. 依赖方向检查（scripts/check_dependencies.py）
  ├── 2. 函数长度检查（scripts/check_function_length.py）
  ├── 3. 技能语法检查（scripts/check_skills.py）
  ├── 4. skill.json 格式检查（scripts/validate_skills.py）
  ├── 5. ruff 代码格式（ruff check）
  └── 6. ruff 代码格式化（ruff format）
  ↓
  全部通过 → 提交成功
  任一失败 → 拒绝提交，Claude Code 需要修复后重试
```

---

**从第一步开始执行，每步验证通过后再继续。**
```