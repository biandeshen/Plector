---
tags: [Plector, 版本, 发布]
type: release
created: 2026-04-08
---

# Plector v1.0 端到端测试（Claude Code 可执行版）

> 本文档验证 Plector 所有已实现功能是否正常工作。
> 共 5 个阶段，按顺序执行，每阶段验证通过后再继续。
>
> 验证范围：
> - 阶段 1：基础模块导入
> - 阶段 2：LLM 对话
> - 阶段 3：技能调用
> - 阶段 4：闭环触发
> - 阶段 5：Harness 检查

---

## 前置条件

```bash
# 确认在项目根目录
cd E:\产品\Plector

# 确认 Ollama 在运行
ollama list

# 确认依赖已安装
pip install -r requirements.txt
```

---

## 阶段 1：基础模块导入

### 1.1 核心模块

```bash
python -c "from core.event_bus import get_event_bus; print('event_bus: OK')"
python -c "from core.skill_registry import SkillRegistry; print('skill_registry: OK')"
python -c "from core.skill_handler import SkillHandler; print('skill_handler: OK')"
python -c "from core.function_calling import ToolRegistry; print('function_calling: OK')"
python -c "from core.closure_engine import ClosureEngine; print('closure_engine: OK')"
python -c "from core.context_builder import ContextBuilder; print('context_builder: OK')"
python -c "from core.governance import Governance; print('governance: OK')"
python -c "from core.llm_client import LLMClient; print('llm_client: OK')"
```

**预期**：8 个模块全部输出 `OK`

### 1.2 AgentLoop 完整初始化

```bash
python -c "
from core.agent_loop import AgentLoop
agent = AgentLoop()
print(f'skills: {len(agent.skill_registry.skills)}')
print(f'tools: {len(agent.tool_registry.get_tool_schemas())}')
print('AgentLoop: OK')
"
```

**预期**：
```
skills: 2
tools: 3
AgentLoop: OK
```

### 1.3 技能详情

```bash
python -c "
from core.skill_registry import SkillRegistry
r = SkillRegistry()
r.scan()
for name, info in r.skills.items():
    tools = info['meta'].get('tools', [])
    print(f'{name}: {len(tools)} tools')
    for t in tools:
        print(f'  - {t[\"name\"]}: {t[\"description\"]}')
"
```

**预期**：
```
health_monitor: 1 tools
  - check_health: 执行健康检查，返回 CPU、内存、磁盘使用率和健康状态
error_knowledge: 2 tools
  - store_error: 存储错误信息到本地知识库
  - classify_error: 分类错误类型，返回分类结果和置信度
```

### 验证通过后提交

```bash
git add -A
git commit -m "test: 阶段 1 基础模块导入验证通过"
```

---

## 阶段 2：LLM 对话

### 2.1 简单对话

```bash
python channels/cli.py --query "你好"
```

**预期**：返回 LLM 的真实回复（非 Echo）

### 2.2 知识问答

```bash
python channels/cli.py --query "1+1等于几"
```

**预期**：返回 "2" 或类似答案

### 2.3 查看 system prompt 是否生效

```bash
python channels/cli.py --query "你有哪些可用的技能？"
```

**预期**：LLM 提到 health_monitor 和 error_knowledge

### 2.4 如果 Ollama 不响应

```bash
# 检查 Ollama 状态
ollama list

# 测试 Ollama 是否正常
ollama run qwen3:4b "你好"

# 如果 Ollama 有问题，用 OpenAI 替代
export OPENAI_API_KEY="sk-xxx"
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    config = {
        'max_iterations': 5,
        'llm': {
            'provider': 'openai',
            'openai': {
                'api_key': '\${OPENAI_API_KEY}',
                'model': 'gpt-4o-mini',
                'base_url': 'https://api.openai.com/v1',
            }
        }
    }
    agent = AgentLoop(config)
    response = await agent.run('你好')
    print(response)

asyncio.run(test())
"
```

### 验证通过后提交

```bash
git add -A
git commit -m "test: 阶段 2 LLM 对话验证通过"
```

---

## 阶段 3：技能调用

### 3.1 直接调用 health_monitor

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('health_monitor', 'check_health', {})
    print(result)

asyncio.run(test())
"
```

**预期**：
```json
{"result": {"success": true, "data": {"cpu": x.x, "memory": x.x, "disk": x.x, "status": "healthy"}, "error": null}}
```

### 3.2 直接调用 error_knowledge

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('error_knowledge', 'store_error', {'error': 'test error from e2e'})
    print(result)

asyncio.run(test())
"
```

**预期**：
```json
{"result": {"success": true, "data": {"error_id": "xxxxxxxx"}, "error": null}}
```

### 3.3 验证错误文件已写入

```bash
ls data/errors/
```

**预期**：看到新生成的 `.json` 文件

### 3.4 查看错误文件内容

```bash
# 替换为实际文件名
cat data/errors/*.json | head -20
```

**预期**：
```json
{
  "id": "xxxxxxxx",
  "error": "test error from e2e",
  "timestamp": "2026-04-04T...",
  "classified": {
    "category": "unknown",
    "confidence": 0.3
  }
}
```

### 3.5 LLM 调用技能（端到端）

```bash
python channels/cli.py --query "请调用 health_monitor_check_health 获取系统健康状态"
```

**预期**：LLM 调用技能，返回真实 CPU/内存/磁盘数据

### 3.6 如果 LLM 没有调用技能

```bash
# 尝试更明确的指令
python channels/cli.py --query "调用 health_monitor 的 check_health 方法，不需要参数"

# 或者用 OpenAI（tool_calling 更强）
export OPENAI_API_KEY="sk-xxx"
python -c "
import asyncio, json
from core.agent_loop import AgentLoop

async def test():
    config = {
        'max_iterations': 5,
        'llm': {
            'provider': 'openai',
            'openai': {
                'api_key': '\${OPENAI_API_KEY}',
                'model': 'gpt-4o-mini',
                'base_url': 'https://api.openai.com/v1',
            }
        }
    }
    agent = AgentLoop(config)
    response = await agent.run('请调用 health_monitor_check_health 获取系统健康状态')
    print(response)

asyncio.run(test())
"
```

### 验证通过后提交

```bash
git add -A
git commit -m "test: 阶段 3 技能调用验证通过"
```

---

## 阶段 4：闭环触发

### 4.1 手动发布事件，触发闭环

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop
from core.event_bus import get_event_bus

async def test():
    # 初始化 AgentLoop（会创建 ClosureEngine，订阅事件）
    agent = AgentLoop()

    # 发布 test.failed 事件
    bus = get_event_bus()
    await bus.publish('test.failed', {'error': 'syntax error in main.py'}, source='e2e_test')

    # 等待异步处理
    await asyncio.sleep(2)
    print('事件已发布，检查 data/errors/ 目录')

asyncio.run(test())
"
```

### 4.2 验证闭环被触发

```bash
ls data/errors/
```

**预期**：看到新生成的 `.json` 文件（闭环触发了 error_knowledge.store_error）

### 4.3 查看闭环生成的错误文件

```bash
# 列出最新的文件
ls -lt data/errors/ | head -5

# 查看内容（替换为实际文件名）
cat data/errors/<最新文件名>.json
```

**预期**：
```json
{
  "id": "xxxxxxxx",
  "error": "syntax error in main.py",
  "timestamp": "2026-04-04T...",
  "classified": {
    "category": "syntax_error",
    "confidence": 0.9
  }
}
```

### 4.4 验证事件格式（CloudEvents）

```bash
python -c "
import asyncio
from core.event_bus import EventBus

async def test():
    bus = EventBus()
    received = []
    async def handler(event):
        received.append(event)
    bus.subscribe('health.degraded', handler)
    await bus.publish('health.degraded', {'cpu': 85.0, 'memory': 90.0}, source='e2e_test')
    await asyncio.sleep(0.5)
    e = received[0]
    print(f'specversion: {e[\"specversion\"]}')
    print(f'id: {e[\"id\"]}')
    print(f'source: {e[\"source\"]}')
    print(f'type: {e[\"type\"]}')
    print(f'time: {e[\"time\"]}')
    print(f'data: {e[\"data\"]}')
    assert e['specversion'] == '1.0'
    print('CloudEvents 格式验证通过')

asyncio.run(test())
"
```

**预期**：输出 CloudEvents 格式字段，最后显示 `CloudEvents 格式验证通过`

### 4.5 验证 JSON-RPC 2.0 错误格式

```bash
python -c "
import asyncio
from core.function_calling import ToolRegistry

async def test():
    t = ToolRegistry()
    # 工具不存在
    result = await t.execute({'function': {'name': 'nonexistent', 'arguments': '{}'}})
    print(f'工具不存在: {result}')
    assert result.get('jsonrpc') == '2.0'
    assert result.get('error', {}).get('code') == -32601

    # JSON 解析错误
    result = await t.execute({'function': {'name': 'test', 'arguments': 'invalid json'}})
    print(f'JSON 解析错误: {result}')
    assert result.get('jsonrpc') == '2.0'
    assert result.get('error', {}).get('code') == -32700

    print('JSON-RPC 2.0 格式验证通过')

asyncio.run(test())
"
```

**预期**：最后显示 `JSON-RPC 2.0 格式验证通过`

### 验证通过后提交

```bash
git add -A
git commit -m "test: 阶段 4 闭环触发验证通过"
```

---

## 阶段 5：Harness 检查

### 5.1 依赖方向

```bash
python scripts/check_dependencies.py
```

**预期**：`✅ 依赖方向检查通过`

### 5.2 函数长度

```bash
python scripts/check_function_length.py
```

**预期**：`✅ 函数长度检查通过`

### 5.3 技能语法

```bash
python scripts/check_skills.py
```

**预期**：exit code 0

### 5.4 skill.json 格式

```bash
python scripts/validate_skills.py
```

**预期**：exit code 0

### 5.5 ruff 代码格式

```bash
ruff check core/ skills/ tools/ channels/
```

**预期**：无错误

### 5.6 mypy 类型检查

```bash
mypy core/ --ignore-missing-imports
```

**预期**：无 error（warning 可忽略）

### 5.7 单元测试

```bash
pytest tests/ -v
```

**预期**：`8 passed`

### 5.8 pre-commit 全部检查

```bash
pre-commit run --all-files
```

**预期**：全部 passed

### 验证通过后提交

```bash
git add -A
git commit -m "test: 阶段 5 Harness 检查全部通过"
```

---

## 最终验收

### 验收清单

```bash
# 运行完整验证脚本
python -c "
import asyncio
import subprocess
import sys

results = []

# 1. 模块导入
try:
    from core.agent_loop import AgentLoop
    agent = AgentLoop()
    results.append(('模块导入', '✅', f'{len(agent.skill_registry.skills)} skills, {len(agent.tool_registry.get_tool_schemas())} tools'))
except Exception as e:
    results.append(('模块导入', '❌', str(e)))

# 2. 技能调用
async def test_skills():
    from core.skill_registry import SkillRegistry
    from core.skill_handler import SkillHandler
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('health_monitor', 'check_health', {})
    if result.get('result', {}).get('success'):
        return '✅', f'CPU: {result[\"result\"][\"data\"][\"cpu\"]}%, Memory: {result[\"result\"][\"data\"][\"memory\"]}%'
    else:
        return '❌', str(result)

try:
    status, detail = asyncio.run(test_skills())
    results.append(('技能调用', status, detail))
except Exception as e:
    results.append(('技能调用', '❌', str(e)))

# 3. CloudEvents 格式
async def test_cloudevents():
    from core.event_bus import EventBus
    bus = EventBus()
    received = []
    async def handler(event):
        received.append(event)
    bus.subscribe('test.final', handler)
    await bus.publish('test.final', {'msg': 'hello'}, source='final_test')
    await asyncio.sleep(0.5)
    e = received[0]
    assert e['specversion'] == '1.0'
    assert e['type'] == 'test.final'
    return '✅', 'CloudEvents 1.0 格式正确'

try:
    status, detail = asyncio.run(test_cloudevents())
    results.append(('CloudEvents', status, detail))
except Exception as e:
    results.append(('CloudEvents', '❌', str(e)))

# 4. JSON-RPC 2.0
async def test_jsonrpc():
    from core.function_calling import ToolRegistry
    t = ToolRegistry()
    result = await t.execute({'function': {'name': 'nonexistent', 'arguments': '{}'}})
    assert result.get('jsonrpc') == '2.0'
    assert result.get('error', {}).get('code') == -32601
    return '✅', 'JSON-RPC 2.0 错误码正确'

try:
    status, detail = asyncio.run(test_jsonrpc())
    results.append(('JSON-RPC 2.0', status, detail))
except Exception as e:
    results.append(('JSON-RPC 2.0', '❌', str(e)))

# 输出结果
print('=' * 60)
print('Plector v1.0 最终验收')
print('=' * 60)
for name, status, detail in results:
    print(f'{status} {name}: {detail}')
print('=' * 60)
passed = sum(1 for _, s, _ in results if s == '✅')
print(f'通过: {passed}/{len(results)}')
"
```

### 预期输出

```
============================================================
Plector v1.0 最终验收
============================================================
✅ 模块导入: 2 skills, 3 tools
✅ 技能调用: CPU: x.x%, Memory: x.x%
✅ CloudEvents: CloudEvents 1.0 格式正确
✅ JSON-RPC 2.0: JSON-RPC 2.0 错误码正确
============================================================
通过: 4/4
```

### 打 tag

```bash
# 推送所有提交
git push

# 打 tag
git tag -a v1.0.0 -m "Plector v1.0.0 - 事件驱动的 AI Agent 引擎

核心功能：
- ReAct 循环 + LLM 多后端（Ollama/OpenAI/Anthropic）
- 技能管理（注册/执行/治理）
- 闭环引擎（条件图执行）
- 事件总线（CloudEvents 1.0）

标准对齐：
- MCP Tool 格式
- OpenAI Function Calling
- CloudEvents 1.0
- JSON-RPC 2.0

Harness：
- 依赖方向检查
- 函数长度检查
- ruff 代码格式
- pre-commit 钩子"

# 推送 tag
git push origin v1.0.0
```

---

## 改动汇总

| 文件 | 操作 |
|------|------|
| 无新文件 | 本次为验证测试，不产生新代码 |

---

**从阶段 1 开始执行，每阶段验证通过后再继续。**
```