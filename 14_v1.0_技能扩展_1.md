---
tags: [Plector, 技能, 扩展]
type: feature
created: 2026-04-08
---

# Plector 技能扩展：code_writer + test_runner（Claude Code 可执行版）

> 本文档为增量更新，Plector v1.0 端到端测试已全部通过。
> 本次更新：添加 2 个核心技能，让闭环真正能"修复"而不只是"记录"。
>
> 技能设计：
> - code_writer：写代码、读代码、修改代码
> - test_runner：运行测试、返回结果
>
> 共 8 步，按顺序执行。

---

## 第一步：创建 code_writer 目录

```bash
mkdir -p skills/code_writer
```

---

## 第二步：创建 code_writer skill.json

```json
{
  "name": "code_writer",
  "description": "代码编写技能，支持写入、读取、修改代码文件",
  "version": "1.0.0",
  "tier": "tier_2_functional",
  "dependencies": [],
  "events_produced": ["code.written", "code.read", "code.modified"],
  "events_consumed": ["code.write_request", "code.read_request"],
  "tools": [
    {
      "name": "write_code",
      "description": "将代码写入指定文件，自动创建目录",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filepath": {
            "type": "string",
            "description": "文件路径，如 data/sort.py"
          },
          "code": {
            "type": "string",
            "description": "要写入的代码内容"
          }
        },
        "required": ["filepath", "code"],
        "additionalProperties": false
      }
    },
    {
      "name": "read_code",
      "description": "读取指定文件的代码内容",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filepath": {
            "type": "string",
            "description": "文件路径"
          }
        },
        "required": ["filepath"],
        "additionalProperties": false
      }
    },
    {
      "name": "modify_code",
      "description": "修改指定文件中的代码，替换 old_text 为 new_text",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filepath": {
            "type": "string",
            "description": "文件路径"
          },
          "old_text": {
            "type": "string",
            "description": "要替换的原文"
          },
          "new_text": {
            "type": "string",
            "description": "替换后的新内容"
          }
        },
        "required": ["filepath", "old_text", "new_text"],
        "additionalProperties": false
      }
    }
  ]
}
```

### 验证

```bash
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/code_writer/skill.json
git commit -m "feat(code_writer): 添加 code_writer 技能元数据（MCP 格式）"
```

---

## 第三步：创建 code_writer implementation.py

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
代码编写技能 - 写入、读取、修改代码文件

功能：
    1. 将代码写入指定文件
    2. 读取文件内容
    3. 修改文件中的指定文本

Author: Plector
Version: 1.0.0
Created: 2026-04-04
"""

import logging
from pathlib import Path
from typing import Any, Dict

from core.event_bus import get_event_bus

logger = logging.getLogger(__name__)


class SkillHandler:
    """代码编写技能处理器"""

    def __init__(self):
        self.name = "code_writer"

    async def write_code(self, filepath: str, code: str) -> Dict[str, Any]:
        """
        将代码写入指定文件

        参数:
            filepath: 文件路径
            code: 代码内容

        返回:
            {"success": bool, "data": {"filepath": str, "lines": int}, "error": str or None}
        """
        try:
            path = Path(filepath)
            path.parent.mkdir(parents=True, exist_ok=True)

            with open(path, "w", encoding="utf-8") as f:
                f.write(code)

            lines = len(code.splitlines())

            bus = get_event_bus()
            await bus.publish("code.written", {
                "filepath": str(path),
                "lines": lines,
            }, source="code_writer")

            return {
                "success": True,
                "data": {"filepath": str(path), "lines": lines},
                "error": None,
            }
        except Exception as e:
            logger.error(f"写入代码失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def read_code(self, filepath: str) -> Dict[str, Any]:
        """
        读取指定文件的代码内容

        参数:
            filepath: 文件路径

        返回:
            {"success": bool, "data": {"filepath": str, "code": str, "lines": int}, "error": str or None}
        """
        try:
            path = Path(filepath)
            if not path.exists():
                return {"success": False, "data": None, "error": f"文件不存在: {filepath}"}

            code = path.read_text(encoding="utf-8")
            lines = len(code.splitlines())

            bus = get_event_bus()
            await bus.publish("code.read", {
                "filepath": str(path),
                "lines": lines,
            }, source="code_writer")

            return {
                "success": True,
                "data": {"filepath": str(path), "code": code, "lines": lines},
                "error": None,
            }
        except Exception as e:
            logger.error(f"读取代码失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def modify_code(self, filepath: str, old_text: str, new_text: str) -> Dict[str, Any]:
        """
        修改指定文件中的代码

        参数:
            filepath: 文件路径
            old_text: 要替换的原文
            new_text: 替换后的新内容

        返回:
            {"success": bool, "data": {"filepath": str, "replacements": int}, "error": str or None}
        """
        try:
            path = Path(filepath)
            if not path.exists():
                return {"success": False, "data": None, "error": f"文件不存在: {filepath}"}

            content = path.read_text(encoding="utf-8")
            if old_text not in content:
                return {"success": False, "data": None, "error": f"未找到要替换的文本"}

            new_content = content.replace(old_text, new_text)
            replacements = content.count(old_text)
            path.write_text(new_content, encoding="utf-8")

            bus = get_event_bus()
            await bus.publish("code.modified", {
                "filepath": str(path),
                "replacements": replacements,
            }, source="code_writer")

            return {
                "success": True,
                "data": {"filepath": str(path), "replacements": replacements},
                "error": None,
            }
        except Exception as e:
            logger.error(f"修改代码失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}
```

### 验证

```bash
python -m py_compile skills/code_writer/implementation.py
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/code_writer/implementation.py
git commit -m "feat(code_writer): 实现 write_code / read_code / modify_code"
```

---

## 第四步：创建 test_runner 目录

```bash
mkdir -p skills/test_runner
```

---

## 第五步：创建 test_runner skill.json

```json
{
  "name": "test_runner",
  "description": "测试运行技能，支持运行 pytest 并返回结果",
  "version": "1.0.0",
  "tier": "tier_2_functional",
  "dependencies": [],
  "events_produced": ["test.passed", "test.failed"],
  "events_consumed": ["test.run_request"],
  "tools": [
    {
      "name": "run_tests",
      "description": "运行 pytest 测试，返回通过/失败结果",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "测试路径，默认 tests/",
            "default": "tests/"
          }
        },
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "run_command",
      "description": "运行任意 shell 命令并返回输出",
      "inputSchema": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "要执行的命令"
          },
          "timeout": {
            "type": "integer",
            "description": "超时时间（秒），默认 30",
            "default": 30
          }
        },
        "required": ["command"],
        "additionalProperties": false
      }
    }
  ]
}
```

### 验证

```bash
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/test_runner/skill.json
git commit -m "feat(test_runner): 添加 test_runner 技能元数据（MCP 格式）"
```

---

## 第六步：创建 test_runner implementation.py

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
测试运行技能 - 运行测试并返回结果

功能：
    1. 运行 pytest 测试
    2. 运行任意 shell 命令
    3. 发布测试事件

Author: Plector
Version: 1.0.0
Created: 2026-04-04
"""

import asyncio
import logging
from typing import Any, Dict

from core.event_bus import get_event_bus

logger = logging.getLogger(__name__)


class SkillHandler:
    """测试运行技能处理器"""

    def __init__(self):
        self.name = "test_runner"

    async def run_tests(self, path: str = "tests/") -> Dict[str, Any]:
        """
        运行 pytest 测试

        参数:
            path: 测试路径，默认 tests/

        返回:
            {"success": bool, "data": {"passed": int, "failed": int, "output": str}, "error": str or None}
        """
        try:
            loop = asyncio.get_event_loop()
            result = await loop.run_in_executor(
                None,
                lambda: self._run_command(f"pytest {path} -v --tb=short", timeout=60)
            )

            passed = result["output"].count(" PASSED")
            failed = result["output"].count(" FAILED")
            total = passed + failed
            success = failed == 0 and total > 0

            bus = get_event_bus()
            if success:
                await bus.publish("test.passed", {
                    "path": path,
                    "passed": passed,
                    "total": total,
                }, source="test_runner")
            else:
                await bus.publish("test.failed", {
                    "path": path,
                    "passed": passed,
                    "failed": failed,
                    "total": total,
                    "output": result["output"],
                }, source="test_runner")

            return {
                "success": success,
                "data": {
                    "passed": passed,
                    "failed": failed,
                    "total": total,
                    "output": result["output"],
                },
                "error": None if success else f"{failed} 个测试失败",
            }
        except Exception as e:
            logger.error(f"运行测试失败: {e}", exc_info=True)

            bus = get_event_bus()
            await bus.publish("test.failed", {
                "path": path,
                "error": str(e),
            }, source="test_runner")

            return {"success": False, "data": None, "error": str(e)}

    async def run_command(self, command: str, timeout: int = 30) -> Dict[str, Any]:
        """
        运行任意 shell 命令

        参数:
            command: 要执行的命令
            timeout: 超时时间（秒）

        返回:
            {"success": bool, "data": {"command": str, "output": str, "returncode": int}, "error": str or None}
        """
        try:
            loop = asyncio.get_event_loop()
            result = await loop.run_in_executor(
                None,
                lambda: self._run_command(command, timeout)
            )

            return {
                "success": result["returncode"] == 0,
                "data": {
                    "command": command,
                    "output": result["output"],
                    "returncode": result["returncode"],
                },
                "error": None if result["returncode"] == 0 else f"命令返回码: {result['returncode']}",
            }
        except Exception as e:
            logger.error(f"运行命令失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    def _run_command(self, command: str, timeout: int) -> Dict[str, Any]:
        """同步执行命令（在线程池中运行）"""
        import subprocess
        try:
            result = subprocess.run(
                command,
                shell=True,
                capture_output=True,
                text=True,
                timeout=timeout,
                encoding="utf-8",
                errors="replace",
            )
            return {
                "output": result.stdout + result.stderr,
                "returncode": result.returncode,
            }
        except subprocess.TimeoutExpired:
            return {
                "output": f"命令超时（{timeout}秒）",
                "returncode": -1,
            }
```

### 验证

```bash
python -m py_compile skills/test_runner/implementation.py
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/test_runner/implementation.py
git commit -m "feat(test_runner): 实现 run_tests / run_command"
```

---

## 第七步：Harness 检查

```bash
# 依赖方向
python scripts/check_dependencies.py

# 函数长度
python scripts/check_function_length.py

# 技能语法
python scripts/check_skills.py

# skill.json 格式
python scripts/validate_skills.py

# ruff
ruff check skills/code_writer/ skills/test_runner/

# pre-commit
pre-commit run --all-files
```

### 修复 ruff 发现的问题

如果有 ruff 错误，运行：

```bash
ruff check skills/code_writer/ skills/test_runner/ --fix
```

### 提交

```bash
git add -A
git commit -m "chore: code_writer + test_runner Harness 检查通过"
```

---

## 第八步：端到端验证

### 8.1 技能加载

```bash
python -c "
from core.skill_registry import SkillRegistry
r = SkillRegistry()
r.scan()
for name, info in r.skills.items():
    tools = info['meta'].get('tools', [])
    print(f'{name}: {len(tools)} tools')
"
```

**预期**：
```
health_monitor: 1 tools
error_knowledge: 2 tools
code_writer: 3 tools
test_runner: 2 tools
```

### 8.2 工具注册

```bash
python -c "
from core.agent_loop import AgentLoop
agent = AgentLoop()
schemas = agent.tool_registry.get_tool_schemas()
for s in schemas:
    print(s['function']['name'])
print(f'总计: {len(schemas)} tools')
"
```

**预期**：
```
health_monitor_check_health
error_knowledge_store_error
error_knowledge_classify_error
code_writer_write_code
code_writer_read_code
code_writer_modify_code
test_runner_run_tests
test_runner_run_command
总计: 8 tools
```

### 8.3 code_writer 写入测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('code_writer', 'write_code', {
        'filepath': 'data/test_output.py',
        'code': 'def hello():\n    return \"Hello, Plector!\"\n'
    })
    print(result)

asyncio.run(test())
"
```

**预期**：
```json
{"result": {"success": true, "data": {"filepath": "data/test_output.py", "lines": 2}, "error": null}}
```

### 8.4 code_writer 读取测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('code_writer', 'read_code', {
        'filepath': 'data/test_output.py'
    })
    print(result['result']['data']['code'])

asyncio.run(test())
"
```

**预期**：
```
def hello():
    return "Hello, Plector!"
```

### 8.5 code_writer 修改测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('code_writer', 'modify_code', {
        'filepath': 'data/test_output.py',
        'old_text': 'Hello, Plector!',
        'new_text': 'Hello, World!'
    })
    print(result)

asyncio.run(test())
"
```

**预期**：
```json
{"result": {"success": true, "data": {"filepath": "data/test_output.py", "replacements": 1}, "error": null}}
```

### 8.6 test_runner 运行测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('test_runner', 'run_tests', {'path': 'tests/'})
    data = result['result']['data']
    print(f'passed: {data[\"passed\"]}')
    print(f'failed: {data[\"failed\"]}')
    print(f'total: {data[\"total\"]}')

asyncio.run(test())
"
```

**预期**：
```
passed: 8
failed: 0
total: 8
```

### 8.7 test_runner 运行命令

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('test_runner', 'run_command', {'command': 'echo hello'})
    print(result)

asyncio.run(test())
"
```

**预期**：
```json
{"result": {"success": true, "data": {"command": "echo hello", "output": "hello\n", "returncode": 0}, "error": null}}
```

### 8.8 LLM 端到端调用

```bash
python channels/cli.py --query "写一个快速排序函数，保存到 data/quicksort.py"
```

**预期**：LLM 调用 code_writer_write_code，文件被写入

### 8.9 验证文件已写入

```bash
cat data/quicksort.py
```

### 8.10 清理测试文件

```bash
rm -f data/test_output.py data/quicksort.py
```

### 最终提交

```bash
git add -A
git commit -m "feat(skills): 添加 code_writer 和 test_runner 技能，闭环可执行修复"
git push
```

---

## 改动汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `skills/code_writer/skill.json` | 新建 | MCP 格式，3 个工具 |
| `skills/code_writer/implementation.py` | 新建 | write_code / read_code / modify_code |
| `skills/test_runner/skill.json` | 新建 | MCP 格式，2 个工具 |
| `skills/test_runner/implementation.py` | 新建 | run_tests / run_command |

## 技能清单（更新后）

| 技能 | 工具数 | 用途 |
|------|--------|------|
| health_monitor | 1 | 系统健康监控 |
| error_knowledge | 2 | 错误记录与分类 |
| code_writer | 3 | 写入、读取、修改代码 |
| test_runner | 2 | 运行测试、运行命令 |
| **总计** | **8** | |

## 闭环能力（更新后）

```
完整闭环示例（test_failure_recovery）：

test.failed
  ↓
error_knowledge.store_error（记录错误）
  ↓
error_knowledge.classify_error（分析错误）
  ↓
code_writer.modify_code（修复代码）
  ↓
test_runner.run_tests（重新测试）
  ↓
测试通过 → 结束
测试失败 → 回到第一步（重试）
```

---

**从第一步开始执行，每步验证通过后再继续。**
```