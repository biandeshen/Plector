---
tags: [Plector, 记忆系统]
type: feature
created: 2026-04-08
---

# Plector 记忆功能实现（Claude Code 可执行版）

> 本次更新：添加记忆功能，让 Agent 能记住对话历史和用户偏好。
>
> 记忆类型：
> - 短期记忆：当前对话历史（按 session_id 隔离）
> - 长期记忆：跨对话的用户偏好
> - 知识记忆：关键词搜索
>
> 存储方式：SQLite（已有 MCP Server）
>
> 已修复的问题：
> - session_id 泄漏（按 session_id 过滤对话历史）
> - 异常处理（降级为空记忆，不影响主流程）
> - 偏好数量限制（最多加载 20 条，避免上下文膨胀）
> - 对话内容截断（预览最多 100 字符）
>
> 共 6 步，按顺序执行。

---

## 第一步：初始化记忆数据库

创建 `servers/init_memory_db.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
初始化记忆数据库

创建记忆相关的表结构
"""

import sqlite3
from pathlib import Path


def init_memory_db(db_path="data/plector.db"):
    """初始化记忆数据库"""
    db = Path(db_path)
    db.parent.mkdir(parents=True, exist_ok=True)

    conn = sqlite3.connect(str(db))
    cursor = conn.cursor()

    # 对话历史表
    cursor.execute("""
        CREATE TABLE IF NOT EXISTS conversations (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            session_id TEXT NOT NULL,
            role TEXT NOT NULL,
            content TEXT NOT NULL,
            timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
        )
    """)

    # 用户偏好表
    cursor.execute("""
        CREATE TABLE IF NOT EXISTS user_preferences (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            key TEXT NOT NULL UNIQUE,
            value TEXT NOT NULL,
            updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
        )
    """)

    # 知识记忆表
    cursor.execute("""
        CREATE TABLE IF NOT EXISTS knowledge (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            topic TEXT NOT NULL,
            content TEXT NOT NULL,
            source TEXT,
            created_at DATETIME DEFAULT CURRENT_TIMESTAMP
        )
    """)

    conn.commit()
    conn.close()
    print(f"记忆数据库初始化完成: {db_path}")


if __name__ == "__main__":
    init_memory_db()
```

### 运行初始化

```bash
python servers/init_memory_db.py
```

### 验证表已创建

```bash
echo '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"list_tables","arguments":{}}}' | python servers/sqlite_server.py data/plector.db
```

**预期**：包含 `conversations`、`user_preferences`、`knowledge` 表

### 提交

```bash
git add servers/init_memory_db.py
git commit -m "feat(memory): 添加记忆数据库初始化脚本"
```

---

## 第二步：创建 memory 技能

### 2.1 skill.json

创建 `skills/memory/skill.json`：

```json
{
  "name": "memory",
  "description": "记忆管理技能，存储和查询对话历史、用户偏好、知识记忆。当用户提到"记住"、"回忆"、"偏好"、"之前聊过"时使用。",
  "version": "1.0.0",
  "tier": "tier_1_system",
  "dependencies": [],
  "events_produced": ["memory.stored", "memory.retrieved"],
  "events_consumed": [],
  "tools": [
    {
      "name": "save_conversation",
      "description": "保存对话记录到记忆数据库",
      "inputSchema": {
        "type": "object",
        "properties": {
          "session_id": {"type": "string", "description": "会话 ID"},
          "role": {"type": "string", "description": "角色（user 或 assistant）"},
          "content": {"type": "string", "description": "对话内容"}
        },
        "required": ["session_id", "role", "content"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_conversation_history",
      "description": "获取指定会话的最近对话历史",
      "inputSchema": {
        "type": "object",
        "properties": {
          "session_id": {"type": "string", "description": "会话 ID"},
          "limit": {"type": "integer", "description": "返回条数"}
        },
        "required": ["session_id", "limit"],
        "additionalProperties": false
      }
    },
    {
      "name": "save_preference",
      "description": "保存用户偏好设置",
      "inputSchema": {
        "type": "object",
        "properties": {
          "key": {"type": "string", "description": "偏好键名"},
          "value": {"type": "string", "description": "偏好值"}
        },
        "required": ["key", "value"],
        "additionalProperties": false
      }
    },
    {
      "name": "get_preference",
      "description": "获取用户偏好设置",
      "inputSchema": {
        "type": "object",
        "properties": {
          "key": {"type": "string", "description": "偏好键名"}
        },
        "required": ["key"],
        "additionalProperties": false
      }
    },
    {
      "name": "save_knowledge",
      "description": "保存知识记忆",
      "inputSchema": {
        "type": "object",
        "properties": {
          "topic": {"type": "string", "description": "主题"},
          "content": {"type": "string", "description": "内容"},
          "source": {"type": "string", "description": "来源"}
        },
        "required": ["topic", "content", "source"],
        "additionalProperties": false
      }
    },
    {
      "name": "search_knowledge",
      "description": "搜索知识记忆",
      "inputSchema": {
        "type": "object",
        "properties": {
          "keyword": {"type": "string", "description": "搜索关键词"}
        },
        "required": ["keyword"],
        "additionalProperties": false
      }
    }
  ]
}
```

### 2.2 implementation.py

创建 `skills/memory/implementation.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
记忆管理技能 - 存储和查询对话历史、用户偏好、知识记忆

功能：
    1. 保存对话记录
    2. 获取对话历史
    3. 保存/获取用户偏好
    4. 保存/搜索知识记忆

Author: Plector
Version: 1.0.0
Created: 2026-04-05
"""

import logging
import sqlite3
from pathlib import Path
from typing import Any, Dict

from core.event_bus import get_event_bus

logger = logging.getLogger(__name__)

DB_PATH = "data/plector.db"


def get_connection():
    """获取数据库连接"""
    db = Path(DB_PATH)
    db.parent.mkdir(parents=True, exist_ok=True)
    return sqlite3.connect(str(db))


class SkillHandler:
    """记忆管理技能处理器"""

    def __init__(self):
        self.name = "memory"

    async def save_conversation(
        self, session_id: str, role: str, content: str
    ) -> Dict[str, Any]:
        """保存对话记录"""
        try:
            conn = get_connection()
            cursor = conn.cursor()
            cursor.execute(
                "INSERT INTO conversations (session_id, role, content) VALUES (?, ?, ?)",
                (session_id, role, content),
            )
            conn.commit()
            conn.close()

            bus = get_event_bus()
            await bus.publish(
                "memory.stored",
                {"type": "conversation", "session_id": session_id},
                source="memory",
            )

            return {"success": True, "data": {"saved": True}, "error": None}
        except Exception as e:
            logger.error(f"保存对话失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def get_conversation_history(
        self, session_id: str, limit=None
    ) -> Dict[str, Any]:
        """获取指定会话的对话历史"""
        try:
            if limit is None:
                limit = 10

            conn = get_connection()
            cursor = conn.cursor()
            cursor.execute(
                "SELECT role, content, timestamp FROM conversations "
                "WHERE session_id = ? ORDER BY timestamp DESC LIMIT ?",
                (session_id, limit),
            )
            rows = cursor.fetchall()
            conn.close()

            messages = [
                {"role": row[0], "content": row[1], "timestamp": row[2]}
                for row in reversed(rows)
            ]

            bus = get_event_bus()
            await bus.publish(
                "memory.retrieved",
                {"type": "conversation", "count": len(messages)},
                source="memory",
            )

            return {"success": True, "data": {"messages": messages}, "error": None}
        except Exception as e:
            logger.error(f"获取对话历史失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def save_preference(self, key: str, value: str) -> Dict[str, Any]:
        """保存用户偏好"""
        try:
            conn = get_connection()
            cursor = conn.cursor()
            cursor.execute(
                "INSERT OR REPLACE INTO user_preferences (key, value, updated_at) "
                "VALUES (?, ?, CURRENT_TIMESTAMP)",
                (key, value),
            )
            conn.commit()
            conn.close()

            return {"success": True, "data": {"saved": True}, "error": None}
        except Exception as e:
            logger.error(f"保存偏好失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def get_preference(self, key: str) -> Dict[str, Any]:
        """获取用户偏好"""
        try:
            conn = get_connection()
            cursor = conn.cursor()
            cursor.execute("SELECT value FROM user_preferences WHERE key = ?", (key,))
            row = cursor.fetchone()
            conn.close()

            if row:
                return {
                    "success": True,
                    "data": {"key": key, "value": row[0]},
                    "error": None,
                }
            return {
                "success": True,
                "data": {"key": key, "value": None},
                "error": None,
            }
        except Exception as e:
            logger.error(f"获取偏好失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def save_knowledge(
        self, topic: str, content: str, source: str
    ) -> Dict[str, Any]:
        """保存知识记忆"""
        try:
            conn = get_connection()
            cursor = conn.cursor()
            cursor.execute(
                "INSERT INTO knowledge (topic, content, source) VALUES (?, ?, ?)",
                (topic, content, source),
            )
            conn.commit()
            conn.close()

            return {"success": True, "data": {"saved": True}, "error": None}
        except Exception as e:
            logger.error(f"保存知识失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def search_knowledge(self, keyword: str) -> Dict[str, Any]:
        """搜索知识记忆"""
        try:
            conn = get_connection()
            cursor = conn.cursor()
            cursor.execute(
                "SELECT topic, content, source, created_at FROM knowledge "
                "WHERE topic LIKE ? OR content LIKE ? "
                "ORDER BY created_at DESC LIMIT 10",
                (f"%{keyword}%", f"%{keyword}%"),
            )
            rows = cursor.fetchall()
            conn.close()

            results = [
                {
                    "topic": row[0],
                    "content": row[1],
                    "source": row[2],
                    "created_at": row[3],
                }
                for row in rows
            ]

            return {"success": True, "data": {"results": results}, "error": None}
        except Exception as e:
            logger.error(f"搜索知识失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}
```

### 2.3 SKILL.md

创建 `skills/memory/SKILL.md`：

```markdown
---
name: memory
description: 记忆管理技能，存储和查询对话历史、用户偏好、知识记忆。当用户提到"记住"、"回忆"、"偏好"、"之前聊过"时使用。
---

# Memory Skill

## 目的
管理 Agent 的记忆系统，包括对话历史、用户偏好和知识记忆。

## 适用场景
- 用户要求记住某些信息
- 用户询问之前聊过的内容
- 用户设置偏好（如语言、格式）
- Agent 需要查询历史知识

## 执行步骤
1. 确认记忆类型（对话/偏好/知识）
2. 执行存储或查询操作
3. 返回结果

## 成功标准
- 对话历史已保存/已查询
- 用户偏好已保存/已获取
- 知识记忆已保存/已搜索

## 相关工具
- `save_conversation`：保存对话记录
- `get_conversation_history`：获取指定会话的对话历史
- `save_preference`：保存用户偏好
- `get_preference`：获取用户偏好
- `save_knowledge`：保存知识记忆
- `search_knowledge`：搜索知识记忆

## 注意事项
- 对话历史按 session_id 隔离，不会跨会话泄漏
- 偏好设置最多加载 20 条，避免上下文膨胀
- 对话内容预览最多 100 字符
- 知识搜索使用 LIKE 模糊匹配
```

### 验证

```bash
python -m py_compile skills/memory/implementation.py
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/memory/
git commit -m "feat(skills): 添加 memory 技能（6 个工具：对话/偏好/知识）"
```

---

## 第三步：修改 AgentLoop 集成记忆

修改 `core/agent_loop.py`。

### 3.1 添加 import

在文件顶部找到 import 区域，添加：

```python
import sqlite3
```

### 3.2 添加记忆方法

在 `AgentLoop` 类中，在 `run` 方法之前添加：

```python
    async def _load_memory(self, session_id: str) -> str:
        """
        加载记忆上下文

        安全机制：
            1. 异常捕获，降级为空记忆（不影响主流程）
            2. 偏好最多加载 20 条（避免上下文膨胀）
            3. 对话按 session_id 过滤（避免跨会话泄漏）
            4. 对话内容截断到 100 字符（节省 token）
        """
        try:
            conn = sqlite3.connect("data/plector.db")
            cursor = conn.cursor()

            memory_parts = []

            # 加载用户偏好（最多 20 条）
            cursor.execute("SELECT key, value FROM user_preferences LIMIT 20")
            prefs = cursor.fetchall()
            if prefs:
                memory_parts.append("## 用户偏好")
                for key, value in prefs:
                    memory_parts.append(f"- {key}: {value}")

            # 加载当前会话的最近对话（按 session_id 过滤）
            cursor.execute(
                "SELECT role, content FROM conversations "
                "WHERE session_id = ? ORDER BY timestamp DESC LIMIT 5",
                (session_id,),
            )
            history = cursor.fetchall()
            conn.close()

            if history:
                memory_parts.append("")
                memory_parts.append("## 最近对话")
                for role, content in reversed(history):
                    if len(content) > 100:
                        preview = content[:100] + "..."
                    else:
                        preview = content
                    memory_parts.append(f"- {role}: {preview}")

            return "\n".join(memory_parts) if memory_parts else ""

        except Exception as e:
            logger.warning(f"加载记忆失败（降级为空）: {e}")
            return ""

    async def _save_conversation(self, session_id: str, role: str, content: str):
        """保存对话记录（静默，失败不影响主流程）"""
        try:
            conn = sqlite3.connect("data/plector.db")
            cursor = conn.cursor()
            cursor.execute(
                "INSERT INTO conversations (session_id, role, content) VALUES (?, ?, ?)",
                (session_id, role, content),
            )
            conn.commit()
            conn.close()
        except Exception as e:
            logger.warning(f"保存对话失败: {e}")
```

### 3.3 修改 run 方法

找到 `run` 方法，替换为：

```python
    async def run(self, user_input: str, session_id: str = "default") -> str:
        """执行 Agent 循环"""
        await self._ensure_mcp_initialized()

        # 保存用户消息
        await self._save_conversation(session_id, "user", user_input)

        # 加载记忆（异常时降级为空）
        memory_context = await self._load_memory(session_id)

        system_prompt = self.context_builder.build_system_prompt()
        if memory_context:
            system_prompt += "\n\n" + memory_context

        messages = [
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_input},
        ]

        for _ in range(self.max_iterations):
            response = await self.llm.chat(
                messages=messages,
                tools=self.tool_registry.get_tool_schemas(),
            )
            if not response.get("tool_calls"):
                # 保存助手回复
                await self._save_conversation(session_id, "assistant", response["content"])
                return response["content"]

            for tool_call in response["tool_calls"]:
                result = await self.tool_registry.execute(tool_call)
                messages.append(
                    {
                        "role": "tool",
                        "tool_call_id": tool_call.get("id"),
                        "content": json.dumps(result),
                    }
                )

        return "达到最大迭代次数"
```

### 验证

```bash
python -c "from core.agent_loop import AgentLoop; print('OK')"
```

### 提交

```bash
git add core/agent_loop.py
git commit -m "feat(core): AgentLoop 集成记忆功能（加载历史 + 保存对话 + 异常降级）"
```

---

## 第四步：Harness 检查

```bash
# 依赖方向
python scripts/check_dependencies.py

# 函数长度
python scripts/check_function_length.py

# 技能语法
python scripts/check_skills.py

# skill.json + SKILL.md 格式
python scripts/validate_skills.py

# ruff
ruff check skills/memory/ core/agent_loop.py
ruff check skills/memory/ core/agent_loop.py --fix

# pre-commit
pre-commit run --all-files
```

### 提交

```bash
git add -A
git commit -m "chore: 记忆功能 Harness 检查通过"
```

---

## 第五步：端到端验证

### 5.1 初始化数据库

```bash
python servers/init_memory_db.py
```

### 5.2 技能加载

```bash
python -c "
from core.skill_registry import SkillRegistry
r = SkillRegistry()
r.scan()
for name, info in r.skills.items():
    tools = info['meta'].get('tools', [])
    print(f'{name}: {len(tools)} tools')
"
```

**预期**：包含 `memory: 6 tools`

### 5.3 保存对话测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('memory', 'save_conversation', {
        'session_id': 'test_session',
        'role': 'user',
        'content': '我叫张三'
    })
    print(result)

asyncio.run(test())
"
```

**预期**：`{"success": true, "data": {"saved": true}}`

### 5.4 获取对话历史测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('memory', 'get_conversation_history', {
        'session_id': 'test_session',
        'limit': 5
    })
    messages = result.get('result', {}).get('data', {}).get('messages', [])
    print(f'消息数: {len(messages)}')
    for m in messages:
        print(f'  {m[\"role\"]}: {m[\"content\"]}')

asyncio.run(test())
"
```

**预期**：返回 1 条消息（我叫张三）

### 5.5 保存偏好测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('memory', 'save_preference', {
        'key': 'name',
        'value': '张三'
    })
    print(result)

asyncio.run(test())
"
```

### 5.6 获取偏好测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('memory', 'get_preference', {
        'key': 'name'
    })
    print(result)

asyncio.run(test())
"
```

**预期**：`{"data": {"key": "name", "value": "张三"}}`

### 5.7 session_id 隔离测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)

    # 保存到 session_a
    await h.execute('memory', 'save_conversation', {
        'session_id': 'session_a',
        'role': 'user',
        'content': '这是 session_a 的消息'
    })

    # 保存到 session_b
    await h.execute('memory', 'save_conversation', {
        'session_id': 'session_b',
        'role': 'user',
        'content': '这是 session_b 的消息'
    })

    # 查询 session_a（不应该看到 session_b 的消息）
    result = await h.execute('memory', 'get_conversation_history', {
        'session_id': 'session_a',
        'limit': 10
    })
    messages = result.get('result', {}).get('data', {}).get('messages', [])
    print(f'session_a 消息数: {len(messages)}')
    for m in messages:
        print(f'  {m[\"role\"]}: {m[\"content\"]}')
        assert 'session_b' not in m['content']
    print('OK: session_id 隔离正确')

asyncio.run(test())
"
```

### 5.8 记忆加载测试

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    agent = AgentLoop()
    memory = await agent._load_memory('test_session')
    print('记忆内容:')
    print(memory)
    print()
    print('OK: 记忆加载正常')

asyncio.run(test())
"
```

### 5.9 异常降级测试

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    agent = AgentLoop()
    # 传入不存在的数据库路径，应该降级为空
    memory = await agent._load_memory('nonexistent')
    print(f'记忆内容: \"{memory}\"')
    print('OK: 异常降级正常')

asyncio.run(test())
"
```

### 5.10 CLI 连续对话测试

```bash
# 第一次对话
python channels/cli.py --query "我叫张三，我喜欢用中文交流"

# 第二次对话（应该记住名字和偏好）
python channels/cli.py --query "我叫什么名字？"
```

### 5.11 清理测试数据

```bash
echo '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"execute","arguments":{"sql":"DELETE FROM conversations WHERE session_id LIKE \"test_\" OR session_id LIKE \"session_\""}}}' | python servers/sqlite_server.py data/plector.db
```

### 最终提交

```bash
git add -A
git commit -m "feat(memory): 记忆功能完整实现并验证通过"
git push
```

---

## 改动汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `servers/init_memory_db.py` | 新建 | 数据库初始化脚本 |
| `skills/memory/skill.json` | 新建 | 记忆技能元数据（6 个工具） |
| `skills/memory/implementation.py` | 新建 | 记忆技能实现 |
| `skills/memory/SKILL.md` | 新建 | 记忆技能说明 |
| `core/agent_loop.py` | 修改 | 集成记忆加载和保存 |

## 安全机制

| 问题 | 修复 |
|------|------|
| session_id 泄漏 | 按 session_id 过滤对话历史 |
| 异常导致崩溃 | try-catch 降级为空记忆 |
| 上下文膨胀 | 偏好最多 20 条，对话最多 5 条 |
| Token 浪费 | 对话预览截断到 100 字符 |

## 工具清单（更新后）

| 技能 | 工具数 | 用途 |
|------|--------|------|
| health_monitor | 1 | 系统健康监控 |
| error_knowledge | 2 | 错误记录与分类 |
| code_writer | 3 | 代码编写 |
| test_runner | 2 | 测试运行 |
| web_search | 2 | 网页搜索 |
| file_utils | 5 | 文件操作 |
| **memory** | **6** | **记忆管理** |
| **总计** | **21 本地工具** | |

## 使用示例

```
用户：我叫张三
  ↓ AgentLoop 自动保存到 conversations 表
  ↓ Agent 识别到名字，调用 save_preference({"key": "name", "value": "张三"})
Agent：你好张三

用户：我叫什么名字？
  ↓ AgentLoop 加载 user_preferences（包含 name: 张三）
  ↓ AgentLoop 加载最近对话（包含"我叫张三"）
Agent：你叫张三
```

---

**从第一步开始执行，每步验证通过后再继续。**
```