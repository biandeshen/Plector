---
tags: [Plector, 核心模块, 实现]
type: feature
created: 2026-04-08
---

# 最终开发指令：实现 Plector 核心模块 + 后续三步（修正版）

## 当前状态

- 已完成 10 个核心模块的代码实现（`core/` 下所有模块）。
- CLI 入口 `channels/cli.py` 可以运行，但 `AgentLoop.run()` 目前是占位（Echo）。
- 没有真实技能（`skills/` 目录为空）。
- 闭环配置只有示例，没有真实逻辑。

## 目标

分三步完成，让 Plector 成为一个可用的 Agent 引擎。

---

## 第一步：集成真实 LLM（让 Agent Loop 能真正工作）

### 任务

修改 `core/agent_loop.py` 的 `run()` 方法，接入真实的大语言模型 API。

### 推荐方案：Ollama（本地免费）

```bash
# 安装 Ollama
curl -fsSL https://ollama.com/install.sh | sh
ollama pull llama3.2
```

### 修改 `core/agent_loop.py`

```python
import asyncio
import json
import ollama
from .skill_registry import SkillRegistry
from .skill_handler import SkillHandler
from .function_calling import ToolRegistry
from .event_bus import get_event_bus

class AgentLoop:
    def __init__(self, config: dict = None):
        self.config = config or {}
        self.skill_registry = SkillRegistry()
        self.skill_registry.scan()
        self.skill_handler = SkillHandler(self.skill_registry)
        self.tool_registry = ToolRegistry()
        self.event_bus = get_event_bus()
        self.max_iterations = self.config.get("max_iterations", 10)
        self.model = self.config.get("model", "llama3.2")
        self._register_skills_as_tools()

    def _register_skills_as_tools(self):
        for skill_name, skill_info in self.skill_registry.skills.items():
            for method_name, method_info in skill_info["meta"].get("methods", {}).items():
                tool_name = f"{skill_name}.{method_name}"
                self.tool_registry.register(
                    name=tool_name,
                    description=method_info.get("description", ""),
                    parameters=method_info.get("params", {}),
                    handler=self._create_skill_handler(skill_name, method_name)
                )

    def _create_skill_handler(self, skill_name, method_name):
        async def handler(**kwargs):
            return await self.skill_handler.execute(skill_name, method_name, kwargs)
        return handler

    async def run(self, user_input: str, session_id: str = None) -> str:
        messages = [{"role": "user", "content": user_input}]
        for _ in range(self.max_iterations):
            # Ollama 目前是同步 API，用线程池执行
            loop = asyncio.get_event_loop()
            response = await loop.run_in_executor(
                None,
                lambda: ollama.chat(
                    model=self.model,
                    messages=messages,
                    tools=self.tool_registry.get_tool_schemas()
                )
            )
            if not response.get("message", {}).get("tool_calls"):
                return response["message"]["content"]
            # 执行工具调用
            for tool_call in response["message"]["tool_calls"]:
                result = await self.tool_registry.execute(tool_call)
                messages.append({
                    "role": "tool",
                    "tool_call_id": tool_call.get("id"),
                    "content": json.dumps(result)
                })
        return "达到最大迭代次数"
```

### 添加配置文件 `config/config.yaml`

```yaml
llm:
  provider: "ollama"
  model: "llama3.2"
  max_iterations: 10
```

### 修改 `AgentLoop` 读取配置

```python
def __init__(self, config_path: str = "config/config.yaml"):
    with open(config_path) as f:
        self.config = yaml.safe_load(f)
    # 其他初始化...
```

### 验证

```bash
python channels/cli.py --query "你好"
# 应返回 LLM 的真实回复
```

---

## 第二步：创建第一批核心技能

### 任务

创建 `health_monitor` 和 `error_knowledge` 两个技能。

### 技能1：`health_monitor`（修正阻塞问题）

**目录结构**：`skills/health_monitor/`

**`skill.json`**：

```json
{
  "name": "health_monitor",
  "description": "获取系统健康状态，包括 CPU、内存、磁盘使用率",
  "tier": "tier_1_system",
  "version": "1.0.0",
  "dependencies": [],
  "events_produced": ["health.degraded", "health.recovered"],
  "events_consumed": [],
  "methods": {
    "check_health": {
      "description": "执行健康检查，无需参数",
      "params": {},
      "returns": {"cpu": "float", "memory": "float", "disk": "float", "status": "string"}
    }
  }
}
```

**`implementation.py`**（修正阻塞，使用 `run_in_executor`）：

```python
import asyncio
import psutil

class SkillHandler:
    async def check_health(self) -> dict:
        loop = asyncio.get_event_loop()
        # 使用线程池执行阻塞调用
        cpu = await loop.run_in_executor(None, lambda: psutil.cpu_percent(interval=0))
        memory = await loop.run_in_executor(None, lambda: psutil.virtual_memory().percent)
        disk = await loop.run_in_executor(None, lambda: psutil.disk_usage('/').percent)
        status = "healthy" if (cpu < 80 and memory < 80 and disk < 80) else "degraded"
        return {"cpu": cpu, "memory": memory, "disk": disk, "status": status}
```

### 技能2：`error_knowledge`

**目录结构**：`skills/error_knowledge/`

**`skill.json`**：

```json
{
  "name": "error_knowledge",
  "description": "记录错误并分类，存储到本地知识库",
  "tier": "tier_2_functional",
  "version": "1.0.0",
  "dependencies": [],
  "events_produced": ["error.classified", "error.stored"],
  "events_consumed": ["test.failed", "skill.failed"],
  "methods": {
    "store_error": {
      "description": "存储错误信息",
      "params": {"error": {"type": "string", "description": "错误描述"}},
      "returns": {"success": "bool", "error_id": "string"}
    },
    "classify_error": {
      "description": "分类错误类型",
      "params": {"error": {"type": "string"}},
      "returns": {"category": "string", "confidence": "float"}
    }
  }
}
```

**`implementation.py`**：

```python
import json
import uuid
from pathlib import Path
from datetime import datetime

class SkillHandler:
    def __init__(self):
        self.errors_dir = Path("data/errors")
        self.errors_dir.mkdir(parents=True, exist_ok=True)

    def store_error(self, error: str) -> dict:
        error_id = str(uuid.uuid4())[:8]
        record = {
            "id": error_id,
            "error": error,
            "timestamp": datetime.now().isoformat(),
            "classified": self.classify_error(error)
        }
        with open(self.errors_dir / f"{error_id}.json", "w") as f:
            json.dump(record, f)
        return {"success": True, "error_id": error_id}

    def classify_error(self, error: str) -> dict:
        error_lower = error.lower()
        if "syntax" in error_lower:
            category = "syntax_error"
        elif "timeout" in error_lower:
            category = "timeout"
        else:
            category = "unknown"
        return {"category": category, "confidence": 0.7}
```

### 验证

```bash
python channels/cli.py --query "调用 health_monitor.check_health"
python channels/cli.py --query "调用 error_knowledge.store_error，参数 error='测试错误'"
```

---

## 第三步：完善闭环配置（仅使用已存在的技能）

### 任务

创建 `config/closed_loops.yaml`，但只引用第二步已实现的技能。

```bash
mkdir -p config
cat > config/closed_loops.yaml << 'EOF'
# 仅使用 health_monitor 和 error_knowledge 的闭环
error_record_loop:
  trigger_on: ["test.failed"]
  entry: "record_error"
  max_iterations: 2
  nodes:
    record_error:
      type: "skill"
      skill: "error_knowledge"
      method: "store_error"
      params_from: "payload"
      next: "classify_error"
    classify_error:
      type: "skill"
      skill: "error_knowledge"
      method: "classify_error"
      next: "end"
    end:
      type: "end"

health_check_loop:
  trigger_on: ["health.degraded"]
  entry: "check_health"
  max_iterations: 3
  nodes:
    check_health:
      type: "skill"
      skill: "health_monitor"
      method: "check_health"
      next: "end"
    end:
      type: "end"
EOF
```

### 验证闭环

```python
# 手动发布 test.failed 事件，观察 error_knowledge 被调用
python -c "
from core.event_bus import get_event_bus
import asyncio
async def test():
    agent = AgentLoop()  # 初始化 ClosureEngine，订阅事件
    bus = get_event_bus()
    await bus.publish('test.failed', {'error': 'syntax error'})
    await asyncio.sleep(1)  # 等待异步处理
    print('检查 data/errors/ 目录')
asyncio.run(test())
"
# 检查 data/errors/ 目录下是否生成错误记录文件
ls data/errors/
```

---

## 完整后续路线图

```
当前（10步完成）→
  ├─ 第一步（1天）：集成 LLM → CLI 能对话
  ├─ 第二步（1天）：创建 health_monitor + error_knowledge → 有实际能力
  ├─ 第三步（0.5天）：完善闭环配置 → 能触发简单闭环
  ├─ 第四步（2天）：编写单元测试，覆盖率 ≥ 80%
  └─ 第五步（1天）：文档完善 + PyPI 发布准备
```

## 验收标准

- [ ] `python channels/cli.py --query "你好"` 返回真实 LLM 回复。
- [ ] `python channels/cli.py --query "调用 health_monitor.check_health"` 返回 CPU/内存/磁盘数据。
- [ ] 发布 `test.failed` 事件后，`error_knowledge.store_error` 被调用，`data/errors/` 下生成文件。
- [ ] 单元测试全部通过，覆盖率 ≥ 80%。

---

**请从第一步开始执行。每完成一步，运行验证命令并报告结果。**