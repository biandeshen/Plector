---
tags: [Plector, MCP, 协议]
type: feature
created: 2026-04-08
---

# Plector Python MCP Server 实现（Claude Code 可执行版）

> 本文档为增量更新，MCP Client 已完成但 MCP Server 无法在 Windows 上启动。
> 本次更新：用 Python 实现 MCP Filesystem Server，不依赖 Node.js。
>
> 共 6 步，按顺序执行。

---

## 第一步：安装 MCP Python SDK

```bash
pip install mcp
```

更新 `requirements.txt`：

```txt
psutil>=5.9.0
pyyaml>=6.0
pytest>=7.0
pytest-asyncio>=0.21
pre-commit>=3.0
ollama>=0.1.0
openai>=1.0.0
anthropic>=0.20.0
ruff>=0.4.0
mypy>=1.10.0
beautifulsoup4>=4.12.0
requests>=2.31.0
mcp>=1.0.0
```

### 验证

```bash
python -c "import mcp; print(f'mcp version: {mcp.__version__}')"
```

### 提交

```bash
git add requirements.txt
git commit -m "chore: 添加 mcp 依赖"
```

---

## 第二步：创建 Python MCP Filesystem Server

```bash
mkdir -p servers
```

创建 `servers/filesystem_server.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Plector MCP Filesystem Server

功能：
    1. 读取文件
    2. 写入文件
    3. 列出目录
    4. 搜索文件
    5. 移动/重命名文件
    6. 获取文件信息

使用方式：
    python servers/filesystem_server.py [根目录]

协议：
    MCP (Model Context Protocol)
    传输：stdio
    格式：JSON-RPC 2.0

Author: Plector
Version: 1.0.0
Created: 2026-04-04
"""

import os
import sys
import json
import glob
from pathlib import Path

from mcp.server import Server
from mcp.server.stdio import stdio_server
from mcp.types import Tool, TextContent


# 从命令行参数获取根目录，默认为当前目录
ROOT_DIR = Path(sys.argv[1]) if len(sys.argv) > 1 else Path(".")
ROOT_DIR = ROOT_DIR.resolve()

# 安全限制
FORBIDDEN_PATHS = ["/", "C:\\", "/etc", "/usr", "/bin", "/sbin"]


def check_safe_path(filepath: str) -> Path:
    """检查路径是否安全"""
    path = (ROOT_DIR / filepath).resolve()
    # 确保路径在根目录内
    if not str(path).startswith(str(ROOT_DIR)):
        raise PermissionError(f"路径超出根目录范围: {filepath}")
    # 确保不操作受保护路径
    resolved = str(path)
    for forbidden in FORBIDDEN_PATHS:
        if resolved == forbidden or resolved.startswith(forbidden + os.sep):
            raise PermissionError(f"禁止操作受保护路径: {resolved}")
    return path


# 创建 MCP Server
server = Server("plector-filesystem")


@server.list_tools()
async def list_tools() -> list[Tool]:
    """列出可用工具"""
    return [
        Tool(
            name="read_file",
            description="读取文件内容",
            inputSchema={
                "type": "object",
                "properties": {
                    "path": {
                        "type": "string",
                        "description": "文件路径（相对于根目录）"
                    },
                    "max_lines": {
                        "type": "integer",
                        "description": "最大行数，默认 200",
                        "default": 200
                    }
                },
                "required": ["path"],
                "additionalProperties": False,
            },
        ),
        Tool(
            name="write_file",
            description="写入文件内容（自动创建目录）",
            inputSchema={
                "type": "object",
                "properties": {
                    "path": {
                        "type": "string",
                        "description": "文件路径（相对于根目录）"
                    },
                    "content": {
                        "type": "string",
                        "description": "文件内容"
                    }
                },
                "required": ["path", "content"],
                "additionalProperties": False,
            },
        ),
        Tool(
            name="list_directory",
            description="列出目录下的文件和子目录",
            inputSchema={
                "type": "object",
                "properties": {
                    "path": {
                        "type": "string",
                        "description": "目录路径，默认 '.'",
                        "default": "."
                    }
                },
                "required": [],
                "additionalProperties": False,
            },
        ),
        Tool(
            name="search_files",
            description="搜索匹配模式的文件",
            inputSchema={
                "type": "object",
                "properties": {
                    "pattern": {
                        "type": "string",
                        "description": "搜索模式，如 '**/*.py'"
                    }
                },
                "required": ["pattern"],
                "additionalProperties": False,
            },
        ),
        Tool(
            name="move_file",
            description="移动或重命名文件",
            inputSchema={
                "type": "object",
                "properties": {
                    "source": {
                        "type": "string",
                        "description": "源路径"
                    },
                    "destination": {
                        "type": "string",
                        "description": "目标路径"
                    }
                },
                "required": ["source", "destination"],
                "additionalProperties": False,
            },
        ),
        Tool(
            name="get_file_info",
            description="获取文件或目录的详细信息",
            inputSchema={
                "type": "object",
                "properties": {
                    "path": {
                        "type": "string",
                        "description": "文件或目录路径"
                    }
                },
                "required": ["path"],
                "additionalProperties": False,
            },
        ),
    ]


@server.call_tool()
async def call_tool(name: str, arguments: dict) -> list[TextContent]:
    """调用工具"""
    try:
        if name == "read_file":
            return await _read_file(arguments)
        elif name == "write_file":
            return await _write_file(arguments)
        elif name == "list_directory":
            return await _list_directory(arguments)
        elif name == "search_files":
            return await _search_files(arguments)
        elif name == "move_file":
            return await _move_file(arguments)
        elif name == "get_file_info":
            return await _get_file_info(arguments)
        else:
            return [TextContent(type="text", text=f"未知工具: {name}")]
    except Exception as e:
        return [TextContent(type="text", text=f"错误: {e}")]


async def _read_file(arguments: dict) -> list[TextContent]:
    """读取文件"""
    filepath = arguments.get("path", "")
    max_lines = arguments.get("max_lines", 200)

    path = check_safe_path(filepath)
    if not path.exists():
        return [TextContent(type="text", text=f"文件不存在: {filepath}")]

    content = path.read_text(encoding="utf-8")
    lines = content.splitlines()

    if len(lines) > max_lines:
        content = "\n".join(lines[:max_lines])
        content += f"\n... (共 {len(lines)} 行，已截断到前 {max_lines} 行)"

    return [TextContent(type="text", text=content)]


async def _write_file(arguments: dict) -> list[TextContent]:
    """写入文件"""
    filepath = arguments.get("path", "")
    content = arguments.get("content", "")

    path = check_safe_path(filepath)
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(content, encoding="utf-8")

    lines = len(content.splitlines())
    return [TextContent(type="text", text=f"已写入 {filepath} ({lines} 行)")]  # noqa: E501


async def _list_directory(arguments: dict) -> list[TextContent]:
    """列出目录"""
    dirpath = arguments.get("path", ".")

    path = check_safe_path(dirpath)
    if not path.exists():
        return [TextContent(type="text", text=f"目录不存在: {dirpath}")]
    if not path.is_dir():
        return [TextContent(type="text", text=f"不是目录: {dirpath}")]

    items = []
    for item in sorted(path.iterdir()):
        if item.is_dir():
            items.append(f"[DIR]  {item.name}/")
        else:
            size = item.stat().st_size
            items.append(f"[FILE] {item.name} ({size} bytes)")

    result = "\n".join(items) if items else "(空目录)"
    return [TextContent(type="text", text=result)]


async def _search_files(arguments: dict) -> list[TextContent]:
    """搜索文件"""
    pattern = arguments.get("pattern", "**/*")

    matches = list(ROOT_DIR.glob(pattern))
    matches = [m for m in matches if m.is_file()]
    matches.sort()

    if not matches:
        return [TextContent(type="text", text=f"未找到匹配 '{pattern}' 的文件")]

    results = []
    for m in matches[:50]:
        rel = m.relative_to(ROOT_DIR)
        size = m.stat().st_size
        results.append(f"{rel} ({size} bytes)")

    if len(matches) > 50:
        results.append(f"... (共 {len(matches)} 个文件，已截断到前 50 个)")

    return [TextContent(type="text", text="\n".join(results))]


async def _move_file(arguments: dict) -> list[TextContent]:
    """移动文件"""
    source = arguments.get("source", "")
    destination = arguments.get("destination", "")

    src = check_safe_path(source)
    dst = check_safe_path(destination)

    if not src.exists():
        return [TextContent(type="text", text=f"源文件不存在: {source}")]

    dst.parent.mkdir(parents=True, exist_ok=True)
    src.rename(dst)

    return [TextContent(type="text", text=f"已移动 {source} → {destination}")]  # noqa: E501


async def _get_file_info(arguments: dict) -> list[TextContent]:
    """获取文件信息"""
    filepath = arguments.get("path", "")

    path = check_safe_path(filepath)
    if not path.exists():
        return [TextContent(type="text", text=f"路径不存在: {filepath}")]

    stat = path.stat()
    info = {
        "name": path.name,
        "path": str(path.relative_to(ROOT_DIR)),
        "type": "directory" if path.is_dir() else "file",
        "size": stat.st_size,
        "modified": stat.st_mtime,
    }

    if path.is_dir():
        items = list(path.iterdir())
        info["children_count"] = len(items)

    return [TextContent(type="text", text=json.dumps(info, indent=2))]


async def main():
    """启动 MCP Server"""
    async with stdio_server() as (read_stream, write_stream):
        await server.run(
            read_stream,
            write_stream,
            server.create_initialization_options(),
        )


if __name__ == "__main__":
    import asyncio
    asyncio.run(main())
```

### 验证

```bash
python -m py_compile servers/filesystem_server.py
```

### 提交

```bash
git add servers/filesystem_server.py
git commit -m "feat(servers): 添加 Python MCP Filesystem Server（不依赖 Node.js）"
```

---

## 第三步：更新 config.yaml

修改 `config/config.yaml` 中的 filesystem 配置：

```yaml
mcp:
  servers:
    # 文件系统 MCP Server（Python 版，不依赖 Node.js）
    filesystem:
      enabled: true
      transport: "stdio"
      command: "python"
      args: ["servers/filesystem_server.py", "."]
      description: "文件系统操作（读写、搜索、目录管理）"

    # GitHub MCP Server（需要 Node.js）
    github:
      enabled: false
      transport: "stdio"
      command: "npx"
      args: ["-y", "@modelcontextprotocol/server-github"]
      env:
        GITHUB_TOKEN: "${GITHUB_TOKEN}"
      description: "GitHub 操作（Issues / PR / Repos）"
```

### 验证

```bash
python -c "
import yaml
with open('config/config.yaml') as f:
    config = yaml.safe_load(f)
fs = config['mcp']['servers']['filesystem']
print(f'command: {fs[\"command\"]}')
print(f'args: {fs[\"args\"]}')
print(f'enabled: {fs[\"enabled\"]}')
"
```

### 提交

```bash
git add config/config.yaml
git commit -m "feat(config): filesystem MCP Server 改用 Python 版（不依赖 Node.js）"
```

---

## 第四步：修复 AgentLoop MCP 懒加载

当前 `_ensure_mcp_initialized` 中的 `connect_all` 可能因为 Windows 路径问题失败。需要捕获更详细的错误信息。

修改 `core/agent_loop.py` 中的 `_ensure_mcp_initialized`：

```python
    async def _ensure_mcp_initialized(self):
        """懒加载 MCP 工具"""
        if self._mcp_initialized:
            return
        try:
            import logging
            logger = logging.getLogger(__name__)
            await self.mcp_client.connect_all()
            all_tools = await self.mcp_client.list_all_tools()
            total = sum(len(tools) for tools in all_tools.values())
            self.mcp_client.register_to_tool_registry(self.tool_registry, all_tools)
            self._mcp_initialized = True
            if total > 0:
                logger.info(f"MCP Client 初始化完成，注册了 {total} 个远程工具")
        except Exception as e:
            import logging
            logger = logging.getLogger(__name__)
            logger.warning(f"MCP Client 初始化失败: {e}")
            self._mcp_initialized = True
```

### 验证

```bash
python -c "from core.agent_loop import AgentLoop; a = AgentLoop(); print('OK')"
```

### 提交

```bash
git add core/agent_loop.py
git commit -m "fix(core): AgentLoop MCP 懒加载添加详细日志"
```

---

## 第五步：Harness 检查

```bash
# 依赖方向
python scripts/check_dependencies.py

# 函数长度
python scripts/check_function_length.py

# 技能语法
python scripts/check_skills.py

# skill.json 格式
python scripts/validate_skills.py

# ruff
ruff check servers/filesystem_server.py
ruff check servers/filesystem_server.py --fix

# pre-commit
pre-commit run --all-files
```

### 提交

```bash
git add -A
git commit -m "chore: Python MCP Server Harness 检查通过"
```

---

## 第六步：端到端验证

### 6.1 MCP Server 独立测试

```bash
python -c "
import asyncio
import json
from servers.filesystem_server import list_tools

async def test():
    tools = await list_tools()
    print(f'发现 {len(tools)} 个工具:')
    for t in tools:
        print(f'  - {t.name}: {t.description}')

asyncio.run(test())
"
```

**预期**：
```
发现 6 个工具:
  - read_file: 读取文件内容
  - write_file: 写入文件内容（自动创建目录）
  - list_directory: 列出目录下的文件和子目录
  - search_files: 搜索匹配模式的文件
  - move_file: 移动或重命名文件
  - get_file_info: 获取文件或目录的详细信息
```

### 6.2 MCP Server 通过 MCPServer 连接测试

```bash
python -c "
import asyncio
from core.mcp_client import MCPServer

async def test():
    server = MCPServer('filesystem', {
        'transport': 'stdio',
        'command': 'python',
        'args': ['servers/filesystem_server.py', '.'],
    })
    await server.connect()
    if server._connected:
        tools = await server.list_tools()
        print(f'MCP Server 连接成功，发现 {len(tools)} 个工具:')
        for t in tools:
            print(f'  - {t[\"name\"]}: {t.get(\"description\", \"\")}')
        await server.disconnect()
    else:
        print('连接失败')

asyncio.run(test())
"
```

**预期**：
```
MCP Server 连接成功，发现 6 个工具:
  - read_file: 读取文件内容
  - write_file: 写入文件内容（自动创建目录）
  - list_directory: 列出目录下的文件和子目录
  - search_files: 搜索匹配模式的文件
  - move_file: 移动或重命名文件
  - get_file_info: 获取文件或目录的详细信息
```

### 6.3 AgentLoop 完整工具列表

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    agent = AgentLoop()
    await agent._ensure_mcp_initialized()
    schemas = agent.tool_registry.get_tool_schemas()
    local_count = 0
    mcp_count = 0
    for s in schemas:
        name = s['function']['name']
        if name.startswith('mcp_'):
            mcp_count += 1
            print(f'  [MCP] {name}')
        else:
            local_count += 1
    print(f'本地工具: {local_count}')
    print(f'MCP 工具: {mcp_count}')
    print(f'总计: {len(schemas)}')

asyncio.run(test())
"
```

**预期**：
```
  [MCP] mcp_filesystem_read_file
  [MCP] mcp_filesystem_write_file
  [MCP] mcp_filesystem_list_directory
  [MCP] mcp_filesystem_search_files
  [MCP] mcp_filesystem_move_file
  [MCP] mcp_filesystem_get_file_info
本地工具: 15
MCP 工具: 6
总计: 21
```

### 6.4 MCP 工具调用测试

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    agent = AgentLoop()
    await agent._ensure_mcp_initialized()

    # 测试 list_directory
    result = await agent.tool_registry.execute({
        'function': {
            'name': 'mcp_filesystem_list_directory',
            'arguments': '{\"path\": \".\"}'
        }
    })
    print('=== list_directory ===')
    print(result.get('result', {}).get('data', {}).get('text', '')[:500])

    # 测试 read_file
    result = await agent.tool_registry.execute({
        'function': {
            'name': 'mcp_filesystem_read_file',
            'arguments': '{\"path\": \"README.md\", \"max_lines\": 5}'
        }
    })
    print('\n=== read_file ===')
    print(result.get('result', {}).get('data', {}).get('text', '')[:200])

asyncio.run(test())
"
```

### 6.5 CLI 端到端测试

```bash
python channels/cli.py --query "列出当前目录下的文件"
```

**预期**：LLM 调用 MCP 工具（mcp_filesystem_list_directory）或本地工具（file_utils_list_files），返回文件列表

### 6.6 安全限制测试

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    agent = AgentLoop()
    await agent._ensure_mcp_initialized()
    result = await agent.tool_registry.execute({
        'function': {
            'name': 'mcp_filesystem_read_file',
            'arguments': '{\"path\": \"../secret.txt\"}'
        }
    })
    print(result)

asyncio.run(test())
"
```

**预期**：返回错误，路径超出根目录范围

### 6.7 关闭 MCP Server

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    agent = AgentLoop()
    await agent._ensure_mcp_initialized()
    await agent.mcp_client.disconnect_all()
    print('MCP Server 已断开')

asyncio.run(test())
"
```

### 最终提交

```bash
git add -A
git commit -m "feat(mcp): Python MCP Filesystem Server 完整实现并验证通过"
git push
```

---

## 改动汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `servers/filesystem_server.py` | 新建 | Python MCP Filesystem Server（6 个工具） |
| `config/config.yaml` | 修改 | filesystem 改用 Python 版 |
| `core/agent_loop.py` | 修改 | MCP 懒加载添加日志 |
| `requirements.txt` | 修改 | 添加 mcp 依赖 |

## 架构变化

```
改动前（Node.js）：
  AgentLoop → MCPClient → npx @modelcontextprotocol/server-filesystem
                          ❌ Windows PATH 问题

改动后（Python）：
  AgentLoop → MCPClient → python servers/filesystem_server.py
                          ✅ 跨平台，无依赖
```

## 工具清单（更新后）

| 来源 | 工具数 | 说明 |
|------|--------|------|
| 本地技能 | 15 | health_monitor / error_knowledge / code_writer / test_runner / web_search / file_utils |
| MCP Server | 6 | read_file / write_file / list_directory / search_files / move_file / get_file_info |
| **总计** | **21** | |

## MCP Server 扩展方式

添加新的 MCP Server 只需要：

```yaml
# config/config.yaml
mcp:
  servers:
    new_server:
      enabled: true
      transport: "stdio"
      command: "python"
      args: ["servers/new_server.py"]
      description: "新服务器描述"
```

然后创建 `servers/new_server.py`，实现 `list_tools` 和 `call_tool`。

---

**从第一步开始执行，每步验证通过后再继续。**
```