---
tags: [Plector, 版本发布, 里程碑]
type: milestone
created: 2026-04-08
---

# HTTP+SSE 已完成

```
v1.2.0 → WebSocket + Dashboard + OpenAI 兼容
v1.3.0 → HTTP+SSE 传输
```

### 打 tag

```bash
git tag -a v1.3.0 -m "Plector v1.3.0 - HTTP+SSE 传输

新增：
- HTTP+SSE MCP Server（远程服务通信）
- MCP Client 支持 stdio + HTTP+SSE 双传输

总计：6 技能，21 工具，10 核心模块，2 渠道，2 传输方式"

git push origin v1.3.0
```

### 验证

```bash
git tag -l
```

**预期**：
```
v1.0.0
v1.1.0
v1.2.0
v1.3.0
```