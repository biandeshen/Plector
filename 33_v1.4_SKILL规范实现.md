---
tags: [Plector, 规范, 设计]
type: spec
created: 2026-04-08
---

# Plector SKILL.md 规范实现（Claude Code 可执行版）

> 本次更新：给 6 个现有技能添加 SKILL.md，对齐 Agent Skills 开放标准。
>
> 共 6 步，按顺序执行。

---

## 第一步：更新 Skill Standard 文档

修改 `docs/standards/Skill_Development_Plector.md`，在末尾添加：

```markdown
## SKILL.md 规范

每个技能目录必须包含 `SKILL.md` 文件，对齐 Agent Skills 开放标准。

### 文件格式

```markdown
---
name: <技能名称（必选，与 skill.json 一致）>
description: <技能描述（必选，包含触发词和使用场景）>
---

# 技能标题

## 目的
简要说明本技能解决什么问题

## 适用场景
- 场景1
- 场景2

## 执行步骤
1. 步骤一
2. 步骤二

## 成功标准
- 标准1
- 标准2

## 相关工具
- `tool_name`：用途说明
```

### 渐进式披露

| 层级 | 内容 | Token | 加载时机 |
|------|------|-------|---------|
| 第一层 | name + description | ~100 | 始终加载 |
| 第二层 | SKILL.md 主体 | <5k | 技能相关时加载 |
| 第三层 | references/ scripts/ | 不限 | 按需加载 |

### 检查清单

- [ ] SKILL.md 存在且包含 YAML frontmatter
- [ ] name 与 skill.json 一致
- [ ] description 包含触发词和使用场景
```

### 提交

```bash
git add docs/standards/Skill_Development_Plector.md
git commit -m "docs: Skill Standard 添加 SKILL.md 规范"
```

---

## 第二步：创建 6 个 SKILL.md

### 2.1 skills/health_monitor/SKILL.md

```markdown
---
name: health_monitor
description: 检查系统健康状态（CPU、内存、磁盘）。当用户询问"系统健康"、"服务器状态"、"资源使用"时使用。
---

# Health Monitor Skill

## 目的
检查系统资源使用情况，返回 CPU、内存、磁盘使用率和健康状态。

## 适用场景
- 用户询问系统是否正常
- 用户询问资源使用情况
- 定期健康检查

## 执行步骤
1. 调用 check_health 工具
2. 返回 CPU、内存、磁盘使用率
3. 判断状态（healthy / degraded）

## 成功标准
- 返回 CPU 使用率
- 返回内存使用率
- 返回磁盘使用率
- 返回健康状态

## 相关工具
- `check_health`：执行健康检查，返回 CPU、内存、磁盘使用率和健康状态
```

### 2.2 skills/error_knowledge/SKILL.md

```markdown
---
name: error_knowledge
description: 记录和分类错误信息。当用户提到"记录错误"、"分析错误"、"错误分类"、"错误日志"时使用。
---

# Error Knowledge Skill

## 目的
存储错误记录并进行分类，帮助分析和追踪问题。

## 适用场景
- 记录新的错误
- 分析错误原因
- 分类错误类型
- 查询历史错误

## 执行步骤
1. 接收错误描述
2. 调用 store_error 存储错误
3. 调用 classify_error 分类错误
4. 返回错误 ID 和分类结果

## 成功标准
- 错误已存储到 data/errors/ 目录
- 返回错误 ID
- 返回错误分类（syntax_error / runtime_error / logic_error / network_error / unknown）

## 相关工具
- `store_error`：存储错误信息到本地知识库
- `classify_error`：分类错误类型，返回分类结果和置信度
```

### 2.3 skills/code_writer/SKILL.md

```markdown
---
name: code_writer
description: 写入、读取、修改代码文件。当用户要求"写代码"、"保存代码"、"读取文件"、"修改代码"时使用。
---

# Code Writer Skill

## 目的
创建和修改代码文件，支持写入、读取、替换操作。

## 适用场景
- 用户要求写一段代码并保存
- 用户要求读取某个文件的内容
- 用户要求修改文件中的某段代码

## 执行步骤
1. 确认文件路径
2. 执行写入/读取/修改操作
3. 返回操作结果

## 成功标准
- 文件已写入/读取/修改
- 返回文件路径和行数

## 相关工具
- `write_code`：将代码写入指定文件，自动创建目录
- `read_code`：读取指定文件的代码内容
- `modify_code`：修改指定文件中的代码，替换 old_text 为 new_text
```

### 2.4 skills/test_runner/SKILL.md

```markdown
---
name: test_runner
description: 运行测试和命令。当用户要求"运行测试"、"跑测试"、"执行命令"时使用。
---

# Test Runner Skill

## 目的
运行 pytest 测试或任意 shell 命令，返回执行结果。

## 适用场景
- 用户要求运行单元测试
- 用户要求执行某个命令
- 代码修改后验证测试是否通过

## 执行步骤
1. 确认测试路径或命令
2. 执行测试/命令
3. 返回执行结果

## 成功标准
- 测试已运行
- 返回通过/失败数量
- 返回命令输出

## 相关工具
- `run_tests`：运行 pytest 测试，返回通过/失败结果
- `run_command`：运行任意 shell 命令并返回输出
```

### 2.5 skills/web_search/SKILL.md

```markdown
---
name: web_search
description: 搜索网页和获取网页内容。当用户要求"搜索"、"查一下"、"获取网页内容"时使用。
---

# Web Search Skill

## 目的
搜索互联网内容，获取网页文本。

## 适用场景
- 用户要求搜索某个主题
- 用户要求获取某个网页的内容
- 需要获取最新信息

## 执行步骤
1. 确认搜索关键词或 URL
2. 执行搜索或获取网页
3. 返回结果

## 成功标准
- 返回搜索结果（标题、链接、摘要）
- 或返回网页文本内容

## 注意事项
- 需要设置 BOCHA_API_KEY 环境变量
- 网络不可用时会失败

## 相关工具
- `search`：搜索网页，返回标题、链接和摘要
- `fetch_page`：获取指定网页的文本内容
```

### 2.6 skills/file_utils/SKILL.md

```markdown
---
name: file_utils
description: 文件操作（列表、复制、移动、删除、读取）。当用户要求"列出文件"、"复制文件"、"移动文件"、"删除文件"时使用。
---

# File Utils Skill

## 目的
安全地操作文件系统，支持列表、复制、移动、删除、读取操作。

## 适用场景
- 用户要求列出目录内容
- 用户要求复制/移动/删除文件
- 用户要求读取文件内容

## 执行步骤
1. 确认操作类型和路径
2. 检查路径安全性
3. 执行操作
4. 返回结果

## 成功标准
- 操作成功完成
- 返回操作结果

## 注意事项
- 禁止操作受保护路径（/、C:\、/etc 等）
- 路径检查通过后才执行操作

## 相关工具
- `list_files`：列出目录下的文件和子目录
- `copy_file`：复制文件到目标路径
- `move_file`：移动文件到目标路径
- `delete_file`：删除指定文件
- `read_file`：读取文件内容
```

### 验证

```bash
# 确认所有文件存在
ls skills/*/SKILL.md
```

**预期**：
```
skills/health_monitor/SKILL.md
skills/error_knowledge/SKILL.md
skills/code_writer/SKILL.md
skills/test_runner/SKILL.md
skills/web_search/SKILL.md
skills/file_utils/SKILL.md
```

### 提交

```bash
git add skills/*/SKILL.md
git commit -m "feat(skills): 添加 6 个 SKILL.md（对齐 Agent Skills 开放标准）"
```

---

## 第三步：更新 validate_skills.py

修改 `scripts/validate_skills.py`，添加 SKILL.md 校验：

找到文件末尾，在 `main()` 函数之前添加：

```python
def validate_skill_md(skill_dir):
    """校验 SKILL.md 文件"""
    errors = []
    skill_md_path = skill_dir / "SKILL.md"

    if not skill_md_path.exists():
        errors.append(f"{skill_dir.name}: 缺少 SKILL.md 文件")
        return errors

    content = skill_md_path.read_text(encoding="utf-8")

    # 检查 YAML frontmatter
    if not content.startswith("---"):
        errors.append(f"{skill_dir.name}/SKILL.md: 缺少 YAML frontmatter")
        return errors

    # 提取 frontmatter
    parts = content.split("---", 2)
    if len(parts) < 3:
        errors.append(f"{skill_dir.name}/SKILL.md: YAML frontmatter 格式错误")
        return errors

    try:
        import yaml
        frontmatter = yaml.safe_load(parts[1])
    except Exception as e:
        errors.append(f"{skill_dir.name}/SKILL.md: YAML 解析错误: {e}")
        return errors

    # 检查必选字段
    if not frontmatter.get("name"):
        errors.append(f"{skill_dir.name}/SKILL.md: 缺少 name 字段")
    if not frontmatter.get("description"):
        errors.append(f"{skill_dir.name}/SKILL.md: 缺少 description 字段")

    # 检查 name 与 skill.json 一致
    skill_json_path = skill_dir / "skill.json"
    if skill_json_path.exists():
        try:
            import json
            with open(skill_json_path, encoding="utf-8") as f:
                skill_json = json.load(f)
            if frontmatter.get("name") != skill_json.get("name"):
                errors.append(
                    f"{skill_dir.name}: SKILL.md name ({frontmatter.get('name')}) "
                    f"与 skill.json name ({skill_json.get('name')}) 不一致"
                )
        except Exception:
            pass

    # 检查 Markdown 内容
    markdown_content = parts[2].strip()
    if len(markdown_content) < 50:
        errors.append(f"{skill_dir.name}/SKILL.md: Markdown 内容过短（< 50 字符）")

    return errors
```

然后修改 `main()` 函数，在现有校验之后添加 SKILL.md 校验：

找到：

```python
def main():
    """主函数"""
    skills_dir = Path("skills")
    errors = []
```

在 `for skill_dir in skills_dir.iterdir():` 循环体末尾添加：

```python
        # 校验 SKILL.md
        skill_md_errors = validate_skill_md(skill_dir)
        errors.extend(skill_md_errors)
```

### 验证

```bash
python scripts/validate_skills.py
```

**预期**：6 个技能全部通过

### 提交

```bash
git add scripts/validate_skills.py
git commit -m "feat(scripts): validate_skills.py 添加 SKILL.md 校验"
```

---

## 第四步：更新 context_builder.py

修改 `core/context_builder.py`，让 LLM 能看到 SKILL.md 的存在：

找到 `build_system_prompt` 方法，在生成 skills 部分时，添加 SKILL.md 路径信息：

找到类似这样的代码：

```python
for name, info in self.skill_registry.skills.items():
    tools = info["meta"].get("tools", [])
    tool_names = ", ".join(t["name"] for t in tools)
    skills_text.append(f"- {name}: {info['meta'].get('description', '')} (工具: {tool_names})")
```

替换为：

```python
for name, info in self.skill_registry.skills.items():
    tools = info["meta"].get("tools", [])
    tool_names = ", ".join(t["name"] for t in tools)
    desc = info["meta"].get("description", "")
    skills_text.append(f"- {name}: {desc} (工具: {tool_names})")
```

在 system prompt 中添加 SKILL.md 说明：

找到生成最终 prompt 的地方，在 skills 列表之后添加：

```python
        # SKILL.md 说明
        prompt_parts.append("")
        prompt_parts.append("## 技能详细说明")
        prompt_parts.append("每个技能目录下有 SKILL.md 文件，包含详细使用说明。")
        prompt_parts.append("当需要了解某个技能的详细用法时，可以读取对应的 SKILL.md 文件。")
        prompt_parts.append("路径格式: skills/<skill_name>/SKILL.md")
```

### 验证

```bash
python -c "
from core.context_builder import ContextBuilder
from core.skill_registry import SkillRegistry
r = SkillRegistry()
r.scan()
c = ContextBuilder(r)
prompt = c.build_system_prompt()
assert 'SKILL.md' in prompt
print('OK: SKILL.md 已加入 system prompt')
"
```

### 提交

```bash
git add core/context_builder.py
git commit -m "feat(core): context_builder 添加 SKILL.md 路径说明"
```

---

## 第五步：Harness 检查

```bash
# 依赖方向
python scripts/check_dependencies.py

# 函数长度
python scripts/check_function_length.py

# 技能语法
python scripts/check_skills.py

# skill.json + SKILL.md 格式
python scripts/validate_skills.py

# ruff
ruff check core/context_builder.py scripts/validate_skills.py
ruff check core/context_builder.py scripts/validate_skills.py --fix

# pre-commit
pre-commit run --all-files
```

### 提交

```bash
git add -A
git commit -m "chore: SKILL.md 规范实现 Harness 检查通过"
```

---

## 第六步：端到端验证

### 6.1 SKILL.md 文件存在

```bash
ls skills/*/SKILL.md | wc -l
```

**预期**：`6`

### 6.2 SKILL.md 格式校验

```bash
python scripts/validate_skills.py
```

**预期**：全部通过

### 6.3 SKILL.md 内容读取

```bash
python -c "
from pathlib import Path
for skill_dir in sorted(Path('skills').iterdir()):
    md = skill_dir / 'SKILL.md'
    if md.exists():
        content = md.read_text(encoding='utf-8')
        lines = content.splitlines()
        name = lines[1].split(': ')[1] if len(lines) > 1 else '?'
        print(f'{skill_dir.name}: {name}')
"
```

**预期**：
```
code_writer: code_writer
error_knowledge: error_knowledge
file_utils: file_utils
health_monitor: health_monitor
test_runner: test_runner
web_search: web_search
```

### 6.4 System Prompt 包含 SKILL.md 说明

```bash
python -c "
from core.context_builder import ContextBuilder
from core.skill_registry import SkillRegistry
r = SkillRegistry()
r.scan()
c = ContextBuilder(r)
prompt = c.build_system_prompt()
print('SKILL.md in prompt:', 'SKILL.md' in prompt)
"
```

### 6.5 LLM 读取 SKILL.md 测试

```bash
python channels/cli.py --query "读取 skills/health_monitor/SKILL.md 文件，告诉我这个技能怎么用"
```

**预期**：LLM 读取 SKILL.md 并解释用法

### 最终提交

```bash
git add -A
git commit -m "feat: SKILL.md 规范完整实现（6 个技能 + 校验 + 加载）"
git push
```

---

## 改动汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `skills/*/SKILL.md` | 新建 × 6 | 对齐 Agent Skills 开放标准 |
| `scripts/validate_skills.py` | 修改 | 添加 SKILL.md 格式校验 |
| `core/context_builder.py` | 修改 | 添加 SKILL.md 路径说明 |
| `docs/standards/Skill_Development_Plector.md` | 修改 | 添加 SKILL.md 规范 |

## 技能目录结构（更新后）

```
skills/
├── health_monitor/
│   ├── SKILL.md          # 新增
│   ├── skill.json
│   └── implementation.py
├── error_knowledge/
│   ├── SKILL.md          # 新增
│   ├── skill.json
│   └── implementation.py
├── code_writer/
│   ├── SKILL.md          # 新增
│   ├── skill.json
│   └── implementation.py
├── test_runner/
│   ├── SKILL.md          # 新增
│   ├── skill.json
│   └── implementation.py
├── web_search/
│   ├── SKILL.md          # 新增
│   ├── skill.json
│   └── implementation.py
└── file_utils/
    ├── SKILL.md          # 新增
    ├── skill.json
    └── implementation.py
```

---

**从第一步开始执行，每步验证通过后再继续。**
```