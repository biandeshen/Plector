---
tags: [Plector, 记忆系统]
type: feature
created: 2026-04-08
---

# Plector 向量数据库语义记忆实现（Claude Code 可执行版）

> 本次更新：添加向量数据库，让 Agent 能语义搜索记忆。
>
> 当前问题：
>   search_knowledge 使用 LIKE 模糊匹配
>   → 只能匹配关键词，不能理解语义
>   → 用户问"怎么处理异常"，搜不到"错误修复"相关内容
>
> 解决方案：
>   使用 ChromaDB（轻量级向量数据库）
>   → 把文本变成向量（数字表示语义）
>   → 搜索时找语义相似的内容
>   → 无需外部服务，本地运行
>
> 共 6 步，按顺序执行。

---

## 第一步：安装依赖

```bash
pip install chromadb
```

更新 `requirements.txt`：

```txt
psutil>=5.9.0
pyyaml>=6.0
pytest>=7.0
pytest-asyncio>=0.21
pre-commit>=3.0
ollama>=0.1.0
openai>=1.0.0
anthropic>=0.20.0
ruff>=0.4.0
mypy>=1.10.0
beautifulsoup4>=4.12.0
requests>=2.31.0
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
sse-starlette>=2.0.0
httpx-sse>=0.4.0
chromadb>=0.5.0
```

### 验证

```bash
python -c "import chromadb; print(f'chromadb {chromadb.__version__}')"
```

### 提交

```bash
git add requirements.txt
git commit -m "chore: 添加 chromadb 依赖（向量数据库）"
```

---

## 第二步：创建向量记忆模块

创建 `core/vector_memory.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
向量记忆模块 - 基于 ChromaDB 的语义记忆

功能：
    1. 将对话、偏好、知识存入向量数据库
    2. 语义搜索相关记忆
    3. 自动管理记忆生命周期

使用方式：
    from core.vector_memory import VectorMemory
    vm = VectorMemory()
    await vm.add("我叫张三", {"type": "preference", "key": "name"})
    results = await vm.search("用户叫什么名字？")

Author: Plector
Version: 1.0.0
Created: 2026-04-05
"""

import logging
import hashlib
from datetime import datetime
from typing import Any, Dict, List, Optional

import chromadb
from chromadb.config import Settings

logger = logging.getLogger(__name__)

DB_PATH = "data/vector_memory"


class VectorMemory:
    """基于 ChromaDB 的向量记忆"""

    def __init__(self, path: str = DB_PATH):
        self.client = chromadb.PersistentClient(
            path=path,
            settings=Settings(anonymized_telemetry=False),
        )

        # 对话记忆集合
        self.conversations = self.client.get_or_create_collection(
            name="conversations",
            metadata={"description": "对话历史的向量索引"},
        )

        # 知识记忆集合
        self.knowledge = self.client.get_or_create_collection(
            name="knowledge",
            metadata={"description": "知识记忆的向量索引"},
        )

        # 用户偏好集合
        self.preferences = self.client.get_or_create_collection(
            name="preferences",
            metadata={"description": "用户偏好的向量索引"},
        )

    def _generate_id(self, text: str, prefix: str = "") -> str:
        """生成唯一 ID"""
        hash_val = hashlib.md5(text.encode()).hexdigest()[:12]
        timestamp = datetime.now().strftime("%Y%m%d%H%M%S")
        return f"{prefix}_{timestamp}_{hash_val}"

    async def add_conversation(
        self,
        text: str,
        session_id: str,
        role: str,
    ) -> str:
        """添加对话记忆"""
        try:
            doc_id = self._generate_id(text, "conv")
            self.conversations.add(
                ids=[doc_id],
                documents=[text],
                metadatas=[{
                    "session_id": session_id,
                    "role": role,
                    "timestamp": datetime.now().isoformat(),
                }],
            )
            return doc_id
        except Exception as e:
            logger.error(f"添加对话向量失败: {e}")
            return ""

    async def add_knowledge(
        self,
        text: str,
        topic: str,
        source: str = "",
    ) -> str:
        """添加知识记忆"""
        try:
            doc_id = self._generate_id(text, "know")
            self.knowledge.add(
                ids=[doc_id],
                documents=[text],
                metadatas=[{
                    "topic": topic,
                    "source": source,
                    "timestamp": datetime.now().isoformat(),
                }],
            )
            return doc_id
        except Exception as e:
            logger.error(f"添加知识向量失败: {e}")
            return ""

    async def add_preference(
        self,
        key: str,
        value: str,
    ) -> str:
        """添加用户偏好"""
        try:
            text = f"{key}: {value}"
            doc_id = f"pref_{key}"
            # 更新已有偏好
            try:
                self.preferences.delete(ids=[doc_id])
            except Exception:
                pass
            self.preferences.add(
                ids=[doc_id],
                documents=[text],
                metadatas=[{
                    "key": key,
                    "value": value,
                    "timestamp": datetime.now().isoformat(),
                }],
            )
            return doc_id
        except Exception as e:
            logger.error(f"添加偏好向量失败: {e}")
            return ""

    async def search(
        self,
        query: str,
        collection: str = "all",
        n_results: int = 5,
        session_id: Optional[str] = None,
    ) -> List[Dict[str, Any]]:
        """
        语义搜索记忆

        参数:
            query: 搜索查询
            collection: 搜索范围（all / conversations / knowledge / preferences）
            n_results: 返回结果数
            session_id: 限制会话范围（仅 conversations）

        返回:
            [{"text": str, "metadata": dict, "distance": float}, ...]
        """
        try:
            results = []

            collections = []
            if collection == "all":
                collections = [
                    ("conversations", self.conversations),
                    ("knowledge", self.knowledge),
                    ("preferences", self.preferences),
                ]
            elif collection == "conversations":
                collections = [("conversations", self.conversations)]
            elif collection == "knowledge":
                collections = [("knowledge", self.knowledge)]
            elif collection == "preferences":
                collections = [("preferences", self.preferences)]

            for name, coll in collections:
                try:
                    # 构建过滤条件
                    where = None
                    if name == "conversations" and session_id:
                        where = {"session_id": session_id}

                    result = coll.query(
                        query_texts=[query],
                        n_results=n_results,
                        where=where,
                    )

                    if result and result["documents"]:
                        for i, doc in enumerate(result["documents"][0]):
                            results.append({
                                "text": doc,
                                "metadata": result["metadatas"][0][i] if result["metadatas"] else {},
                                "distance": result["distances"][0][i] if result["distances"] else 0,
                                "collection": name,
                            })
                except Exception as e:
                    logger.warning(f"搜索 {name} 失败: {e}")

            # 按距离排序（距离越小越相关）
            results.sort(key=lambda x: x["distance"])
            return results[:n_results]

        except Exception as e:
            logger.error(f"语义搜索失败: {e}")
            return []

    async def get_stats(self) -> Dict[str, int]:
        """获取记忆统计"""
        return {
            "conversations": self.conversations.count(),
            "knowledge": self.knowledge.count(),
            "preferences": self.preferences.count(),
        }

    async def delete_session(self, session_id: str):
        """删除指定会话的所有记忆"""
        try:
            result = self.conversations.get(
                where={"session_id": session_id}
            )
            if result and result["ids"]:
                self.conversations.delete(ids=result["ids"])
        except Exception as e:
            logger.error(f"删除会话记忆失败: {e}")
```

### 验证

```bash
python -c "from core.vector_memory import VectorMemory; print('OK')"
```

### 提交

```bash
git add core/vector_memory.py
git commit -m "feat(core): 添加向量记忆模块（ChromaDB 语义搜索）"
```

---

## 第三步：更新 memory 技能

### 3.1 更新 skill.json

修改 `skills/memory/skill.json`，在 tools 中添加：

```json
    {
      "name": "semantic_search",
      "description": "语义搜索记忆，理解查询意图而非关键词匹配",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {"type": "string", "description": "搜索查询"},
          "collection": {"type": "string", "description": "搜索范围（all / conversations / knowledge / preferences）"},
          "limit": {"type": "integer", "description": "返回结果数"}
        },
        "required": ["query", "collection", "limit"],
        "additionalProperties": false
      }
    },
    {
      "name": "memory_stats",
      "description": "获取记忆统计信息",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
```

### 3.2 更新 implementation.py

在 `skills/memory/implementation.py` 中：

添加 import：

```python
from core.vector_memory import VectorMemory
```

在 `SkillHandler.__init__` 中添加：

```python
    def __init__(self):
        self.name = "memory"
        self.vector_memory = VectorMemory()
```

在类末尾添加 2 个新方法：

```python
    async def semantic_search(
        self, query: str, collection=None, limit=None
    ) -> Dict[str, Any]:
        """语义搜索记忆"""
        try:
            if collection is None:
                collection = "all"
            if limit is None:
                limit = 5

            results = await self.vector_memory.search(
                query=query,
                collection=collection,
                n_results=limit,
            )

            return {
                "success": True,
                "data": {"results": results, "count": len(results)},
                "error": None,
            }
        except Exception as e:
            logger.error(f"语义搜索失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def memory_stats(self) -> Dict[str, Any]:
        """获取记忆统计"""
        try:
            stats = await self.vector_memory.get_stats()
            return {
                "success": True,
                "data": stats,
                "error": None,
            }
        except Exception as e:
            logger.error(f"获取统计失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}
```

修改 `save_conversation`，在保存到 SQLite 后同时存入向量库：

```python
    async def save_conversation(
        self, session_id: str, role: str, content: str
    ) -> Dict[str, Any]:
        """保存对话记录"""
        try:
            conn = get_connection()
            cursor = conn.cursor()
            cursor.execute(
                "INSERT INTO conversations (session_id, role, content) VALUES (?, ?, ?)",
                (session_id, role, content),
            )
            conn.commit()
            conn.close()

            # 同时存入向量库
            await self.vector_memory.add_conversation(
                text=content,
                session_id=session_id,
                role=role,
            )

            bus = get_event_bus()
            await bus.publish(
                "memory.stored",
                {"type": "conversation", "session_id": session_id},
                source="memory",
            )

            return {"success": True, "data": {"saved": True}, "error": None}
        except Exception as e:
            logger.error(f"保存对话失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}
```

修改 `save_preference`，在保存到 SQLite 后同时存入向量库：

```python
    async def save_preference(self, key: str, value: str) -> Dict[str, Any]:
        """保存用户偏好"""
        try:
            conn = get_connection()
            cursor = conn.cursor()
            cursor.execute(
                "INSERT OR REPLACE INTO user_preferences (key, value, updated_at) "
                "VALUES (?, ?, CURRENT_TIMESTAMP)",
                (key, value),
            )
            conn.commit()
            conn.close()

            # 同时存入向量库
            await self.vector_memory.add_preference(key=key, value=value)

            return {"success": True, "data": {"saved": True}, "error": None}
        except Exception as e:
            logger.error(f"保存偏好失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}
```

修改 `save_knowledge`，在保存到 SQLite 后同时存入向量库：

```python
    async def save_knowledge(
        self, topic: str, content: str, source: str
    ) -> Dict[str, Any]:
        """保存知识记忆"""
        try:
            conn = get_connection()
            cursor = conn.cursor()
            cursor.execute(
                "INSERT INTO knowledge (topic, content, source) VALUES (?, ?, ?)",
                (topic, content, source),
            )
            conn.commit()
            conn.close()

            # 同时存入向量库
            await self.vector_memory.add_knowledge(
                text=content,
                topic=topic,
                source=source,
            )

            return {"success": True, "data": {"saved": True}, "error": None}
        except Exception as e:
            logger.error(f"保存知识失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}
```

### 3.3 更新 SKILL.md

修改 `skills/memory/SKILL.md`，添加新工具说明：

```markdown
## 相关工具
- `save_conversation`：保存对话记录（同时存入 SQLite 和向量库）
- `get_conversation_history`：获取指定会话的对话历史
- `save_preference`：保存用户偏好（同时存入 SQLite 和向量库）
- `get_preference`：获取用户偏好
- `save_knowledge`：保存知识记忆（同时存入 SQLite 和向量库）
- `search_knowledge`：关键词搜索知识（LIKE 匹配）
- `semantic_search`：语义搜索记忆（向量相似度匹配，推荐使用）
- `memory_stats`：获取记忆统计信息

## 搜索方式对比

| 方式 | 工具 | 原理 | 适用场景 |
|------|------|------|---------|
| 关键词搜索 | search_knowledge | LIKE 模糊匹配 | 知道精确关键词 |
| 语义搜索 | semantic_search | 向量相似度 | 只知道大概意思 |
```

### 验证

```bash
python -m py_compile skills/memory/implementation.py
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/memory/
git commit -m "feat(memory): 添加语义搜索（semantic_search + memory_stats）"
```

---

## 第四步：修改 AgentLoop 使用语义搜索

修改 `core/agent_loop.py` 中的 `_load_memory`：

```python
    async def _load_memory(self, session_id: str) -> str:
        """
        加载记忆上下文（语义搜索版）

        安全机制：
            1. 异常捕获，降级为空记忆
            2. 偏好最多加载 20 条
            3. 对话按 session_id 过滤
            4. 对话内容截断到 100 字符
        """
        try:
            from core.vector_memory import VectorMemory

            memory_parts = []
            vm = VectorMemory()

            # 语义搜索相关偏好
            pref_results = await vm.search(
                query=session_id,
                collection="preferences",
                n_results=20,
            )
            if pref_results:
                memory_parts.append("## 用户偏好")
                for r in pref_results:
                    memory_parts.append(f"- {r['text']}")

            # 语义搜索当前会话的相关对话
            conv_results = await vm.search(
                query="最近的对话内容",
                collection="conversations",
                n_results=5,
                session_id=session_id,
            )
            if conv_results:
                memory_parts.append("")
                memory_parts.append("## 最近对话")
                for r in conv_results:
                    content = r["text"]
                    if len(content) > 100:
                        content = content[:100] + "..."
                    memory_parts.append(f"- {r['metadata'].get('role', 'unknown')}: {content}")

            return "\n".join(memory_parts) if memory_parts else ""

        except Exception as e:
            logger.warning(f"加载记忆失败（降级为空）: {e}")
            return ""
```

### 验证

```bash
python -c "from core.agent_loop import AgentLoop; print('OK')"
```

### 提交

```bash
git add core/agent_loop.py
git commit -m "feat(core): AgentLoop 使用语义搜索加载记忆"
```

---

## 第五步：Harness 检查

```bash
# 依赖方向
python scripts/check_dependencies.py

# 函数长度
python scripts/check_function_length.py

# 技能语法
python scripts/check_skills.py

# skill.json + SKILL.md 格式
python scripts/validate_skills.py

# ruff
ruff check core/vector_memory.py skills/memory/implementation.py core/agent_loop.py
ruff check core/vector_memory.py skills/memory/implementation.py core/agent_loop.py --fix

# pre-commit
pre-commit run --all-files
```

### 提交

```bash
git add -A
git commit -m "chore: 向量记忆 Harness 检查通过"
```

---

## 第六步：端到端验证

### 6.1 向量记忆模块测试

```bash
python -c "
import asyncio
from core.vector_memory import VectorMemory

async def test():
    vm = VectorMemory()

    # 添加测试数据
    await vm.add_conversation('我叫张三，我喜欢 Python', 'test', 'user')
    await vm.add_conversation('你好张三，Python 很棒', 'test', 'assistant')
    await vm.add_preference('name', '张三')
    await vm.add_preference('language', 'Python')
    await vm.add_knowledge('Plector 是一个 AI Agent 引擎', '项目介绍', 'README')

    # 语义搜索
    results = await vm.search('用户叫什么名字？')
    print(f'搜索结果: {len(results)} 条')
    for r in results:
        print(f'  [{r[\"collection\"]}] {r[\"text\"][:50]} (距离: {r[\"distance\"]:.3f})')

    # 统计
    stats = await vm.get_stats()
    print(f'统计: {stats}')

asyncio.run(test())
"
```

### 6.2 memory 技能语义搜索测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)

    # 语义搜索
    result = await h.execute('memory', 'semantic_search', {
        'query': '用户喜欢什么编程语言？',
        'collection': 'all',
        'limit': 3
    })
    data = result.get('result', {}).get('data', {})
    print(f'结果数: {data.get(\"count\", 0)}')
    for item in data.get('results', []):
        print(f'  [{item[\"collection\"]}] {item[\"text\"][:50]}')

asyncio.run(test())
"
```

### 6.3 记忆统计测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)

    result = await h.execute('memory', 'memory_stats', {})
    print(result)

asyncio.run(test())
"
```

### 6.4 CLI 语义搜索测试

```bash
python channels/cli.py --query "我之前聊过什么关于 Python 的内容？"
```

### 6.5 清理测试数据

```bash
python -c "
import asyncio
from core.vector_memory import VectorMemory

async def test():
    vm = VectorMemory()
    await vm.delete_session('test')
    stats = await vm.get_stats()
    print(f'清理后统计: {stats}')

asyncio.run(test())
"
```

### 最终提交

```bash
git add -A
git commit -m "feat(memory): 向量数据库语义记忆完整实现并验证通过"
git push
```

---

## 改动汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `core/vector_memory.py` | 新建 | 向量记忆模块（ChromaDB） |
| `skills/memory/skill.json` | 修改 | 添加 semantic_search + memory_stats |
| `skills/memory/implementation.py` | 修改 | 添加语义搜索 + 双写（SQLite + 向量库） |
| `skills/memory/SKILL.md` | 修改 | 添加语义搜索说明 |
| `core/agent_loop.py` | 修改 | 使用语义搜索加载记忆 |
| `requirements.txt` | 修改 | 添加 chromadb |

## 工具清单（更新后）

| 技能 | 工具数 | 用途 |
|------|--------|------|
| health_monitor | 1 | 系统健康监控 |
| error_knowledge | 2 | 错误记录与分类 |
| code_writer | 3 | 代码编写 |
| test_runner | 2 | 测试运行 |
| web_search | 2 | 网页搜索 |
| file_utils | 5 | 文件操作 |
| **memory** | **8** | **记忆管理（+2 语义搜索）** |
| MCP: filesystem | 6 | 文件操作 |
| MCP: sqlite | 4 | 数据库操作 |
| **总计** | **33** | |

## 记忆架构

```
用户输入
  ↓
AgentLoop.run()
  ↓
┌─────────────────────────────────────┐
│           记忆加载                    │
│  ┌──────────┐  ┌──────────────────┐ │
│  │ SQLite   │  │ ChromaDB         │ │
│  │ 精确查询  │  │ 语义搜索         │ │
│  │ (key=xxx)│  │ (意思相近)       │ │
│  └──────────┘  └──────────────────┘ │
└─────────────────────────────────────┘
  ↓
System Prompt（包含记忆上下文）
  ↓
LLM 推理
  ↓
┌─────────────────────────────────────┐
│           记忆保存                    │
│  ┌──────────┐  ┌──────────────────┐ │
│  │ SQLite   │  │ ChromaDB         │ │
│  │ 结构化存储 │  │ 向量化存储       │ │
│  └──────────┘  └──────────────────┘ │
└─────────────────────────────────────┘
```

## 语义搜索 vs 关键词搜索

```
用户问："怎么处理程序出错？"

关键词搜索（LIKE）：
  → 搜索包含"处理"和"出错"的记录
  → 找不到"错误修复"、"异常捕获"

语义搜索（向量）：
  → 理解"处理出错"的意思
  → 找到"错误修复"、"异常捕获"、"try except"
  → 因为它们语义相近
```

---

**从第一步开始执行，每步验证通过后再继续。**
```