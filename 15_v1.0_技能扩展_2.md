---
tags: [Plector, 技能, 扩展]
type: feature
created: 2026-04-08
---

# Plector 技能扩展：web_search + file_utils（Claude Code 可执行版）

> 本文档为增量更新，code_writer 和 test_runner 已完成。
> 本次更新：添加 2 个实用技能。
>
> 技能设计：
> - web_search：网页搜索（DuckDuckGo，无需 API Key）
> - file_utils：文件操作（列表、复制、移动、删除）
>
> 共 8 步，按顺序执行。

---

## 第一步：创建 web_search 目录和 skill.json

```bash
mkdir -p skills/web_search
```

```json
{
  "name": "web_search",
  "description": "网页搜索技能，使用 DuckDuckGo 搜索互联网内容",
  "version": "1.0.0",
  "tier": "tier_2_functional",
  "dependencies": [],
  "events_produced": ["search.completed"],
  "events_consumed": [],
  "tools": [
    {
      "name": "search",
      "description": "搜索网页，返回标题、链接和摘要",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "搜索关键词"
          },
          "max_results": {
            "type": "integer",
            "description": "最大结果数，默认 5",
            "default": 5
          }
        },
        "required": ["query"],
        "additionalProperties": false
      }
    },
    {
      "name": "fetch_page",
      "description": "获取指定网页的文本内容",
      "inputSchema": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "description": "网页 URL"
          }
        },
        "required": ["url"],
        "additionalProperties": false
      }
    }
  ]
}
```

### 验证

```bash
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/web_search/skill.json
git commit -m "feat(web_search): 添加 web_search 技能元数据（MCP 格式）"
```

---

## 第二步：创建 web_search implementation.py

```bash
pip install duckduckgo-search beautifulsoup4 requests
```

更新 `requirements.txt`，添加：

```
duckduckgo-search>=6.0.0
beautifulsoup4>=4.12.0
requests>=2.31.0
```

创建 `skills/web_search/implementation.py`：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
网页搜索技能 - 搜索互联网内容

功能：
    1. 使用 DuckDuckGo 搜索网页
    2. 获取网页文本内容

Author: Plector
Version: 1.0.0
Created: 2026-04-04
"""

import asyncio
import logging
from typing import Any, Dict

from core.event_bus import get_event_bus

logger = logging.getLogger(__name__)


class SkillHandler:
    """网页搜索技能处理器"""

    def __init__(self):
        self.name = "web_search"

    async def search(self, query: str, max_results: int = 5) -> Dict[str, Any]:
        """
        搜索网页

        参数:
            query: 搜索关键词
            max_results: 最大结果数

        返回:
            {"success": bool, "data": {"results": [...]}, "error": str or None}
        """
        try:
            from duckduckgo_search import DDGS

            loop = asyncio.get_event_loop()
            results = await loop.run_in_executor(
                None,
                lambda: self._search_sync(query, max_results)
            )

            bus = get_event_bus()
            await bus.publish("search.completed", {
                "query": query,
                "result_count": len(results),
            }, source="web_search")

            return {
                "success": True,
                "data": {"results": results, "query": query},
                "error": None,
            }
        except Exception as e:
            logger.error(f"搜索失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    def _search_sync(self, query: str, max_results: int) -> list:
        """同步搜索（在线程池中运行）"""
        from duckduckgo_search import DDGS
        with DDGS() as ddgs:
            results = list(ddgs.text(query, max_results=max_results))
        return [
            {
                "title": r.get("title", ""),
                "url": r.get("href", ""),
                "snippet": r.get("body", ""),
            }
            for r in results
        ]

    async def fetch_page(self, url: str) -> Dict[str, Any]:
        """
        获取网页文本内容

        参数:
            url: 网页 URL

        返回:
            {"success": bool, "data": {"url": str, "text": str}, "error": str or None}
        """
        try:
            import requests
            from bs4 import BeautifulSoup

            loop = asyncio.get_event_loop()
            result = await loop.run_in_executor(
                None,
                lambda: self._fetch_page_sync(url)
            )

            return {
                "success": True,
                "data": result,
                "error": None,
            }
        except Exception as e:
            logger.error(f"获取网页失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    def _fetch_page_sync(self, url: str) -> Dict[str, Any]:
        """同步获取网页（在线程池中运行）"""
        import requests
        from bs4 import BeautifulSoup

        response = requests.get(url, timeout=10, headers={
            "User-Agent": "Mozilla/5.0 (Plector/1.0)"
        })
        response.raise_for_status()

        soup = BeautifulSoup(response.text, "html.parser")
        # 移除脚本和样式
        for tag in soup(["script", "style", "nav", "footer"]):
            tag.decompose()
        text = soup.get_text(separator="\n", strip=True)
        # 截断过长内容
        if len(text) > 5000:
            text = text[:5000] + "\n... (内容已截断)"

        return {"url": url, "text": text}
```

### 验证

```bash
python -m py_compile skills/web_search/implementation.py
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/web_search/implementation.py requirements.txt
git commit -m "feat(web_search): 实现 search / fetch_page（DuckDuckGo + BeautifulSoup）"
```

---

## 第三步：创建 file_utils 目录和 skill.json

```bash
mkdir -p skills/file_utils
```

```json
{
  "name": "file_utils",
  "description": "文件操作技能，支持列表、复制、移动、删除文件",
  "version": "1.0.0",
  "tier": "tier_2_functional",
  "dependencies": [],
  "events_produced": ["file.copied", "file.moved", "file.deleted"],
  "events_consumed": [],
  "tools": [
    {
      "name": "list_files",
      "description": "列出目录下的文件和子目录",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "目录路径，默认当前目录",
            "default": "."
          },
          "pattern": {
            "type": "string",
            "description": "文件匹配模式，如 *.py",
            "default": "*"
          }
        },
        "required": [],
        "additionalProperties": false
      }
    },
    {
      "name": "copy_file",
      "description": "复制文件到目标路径",
      "inputSchema": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "description": "源文件路径"
          },
          "destination": {
            "type": "string",
            "description": "目标文件路径"
          }
        },
        "required": ["source", "destination"],
        "additionalProperties": false
      }
    },
    {
      "name": "move_file",
      "description": "移动文件到目标路径",
      "inputSchema": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "description": "源文件路径"
          },
          "destination": {
            "type": "string",
            "description": "目标文件路径"
          }
        },
        "required": ["source", "destination"],
        "additionalProperties": false
      }
    },
    {
      "name": "delete_file",
      "description": "删除指定文件",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filepath": {
            "type": "string",
            "description": "要删除的文件路径"
          }
        },
        "required": ["filepath"],
        "additionalProperties": false
      }
    },
    {
      "name": "read_file",
      "description": "读取文件内容（通用，不限于代码）",
      "inputSchema": {
        "type": "object",
        "properties": {
          "filepath": {
            "type": "string",
            "description": "文件路径"
          },
          "max_lines": {
            "type": "integer",
            "description": "最大行数，默认 100",
            "default": 100
          }
        },
        "required": ["filepath"],
        "additionalProperties": false
      }
    }
  ]
}
```

### 验证

```bash
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/file_utils/skill.json
git commit -m "feat(file_utils): 添加 file_utils 技能元数据（MCP 格式）"
```

---

## 第四步：创建 file_utils implementation.py

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
文件操作技能 - 列表、复制、移动、删除、读取文件

功能：
    1. 列出目录下的文件
    2. 复制文件
    3. 移动文件
    4. 删除文件
    5. 读取文件内容

Author: Plector
Version: 1.0.0
Created: 2026-04-04
"""

import logging
import shutil
from pathlib import Path
from typing import Any, Dict, List

from core.event_bus import get_event_bus

logger = logging.getLogger(__name__)

# 安全限制：禁止操作的目录
FORBIDDEN_PATHS = ["/", "C:\\", "/etc", "/usr", "/bin", "/sbin"]


class SkillHandler:
    """文件操作技能处理器"""

    def __init__(self):
        self.name = "file_utils"

    async def list_files(self, path: str = ".", pattern: str = "*") -> Dict[str, Any]:
        """
        列出目录下的文件

        参数:
            path: 目录路径
            pattern: 文件匹配模式

        返回:
            {"success": bool, "data": {"files": [...], "dirs": [...]}, "error": str or None}
        """
        try:
            dir_path = Path(path)
            if not dir_path.exists():
                return {"success": False, "data": None, "error": f"目录不存在: {path}"}
            if not dir_path.is_dir():
                return {"success": False, "data": None, "error": f"不是目录: {path}"}

            files = []
            dirs = []
            for item in sorted(dir_path.glob(pattern)):
                if item.is_file():
                    files.append({
                        "name": item.name,
                        "path": str(item),
                        "size": item.stat().st_size,
                    })
                elif item.is_dir():
                    dirs.append({
                        "name": item.name,
                        "path": str(item),
                    })

            return {
                "success": True,
                "data": {"files": files, "dirs": dirs, "path": str(dir_path)},
                "error": None,
            }
        except Exception as e:
            logger.error(f"列出文件失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def copy_file(self, source: str, destination: str) -> Dict[str, Any]:
        """
        复制文件

        参数:
            source: 源文件路径
            destination: 目标文件路径

        返回:
            {"success": bool, "data": {"source": str, "destination": str}, "error": str or None}
        """
        try:
            src = Path(source)
            dst = Path(destination)

            if not src.exists():
                return {"success": False, "data": None, "error": f"源文件不存在: {source}"}

            self._check_safe_path(dst)
            dst.parent.mkdir(parents=True, exist_ok=True)
            shutil.copy2(src, dst)

            bus = get_event_bus()
            await bus.publish("file.copied", {
                "source": str(src),
                "destination": str(dst),
            }, source="file_utils")

            return {
                "success": True,
                "data": {"source": str(src), "destination": str(dst)},
                "error": None,
            }
        except Exception as e:
            logger.error(f"复制文件失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def move_file(self, source: str, destination: str) -> Dict[str, Any]:
        """
        移动文件

        参数:
            source: 源文件路径
            destination: 目标文件路径

        返回:
            {"success": bool, "data": {"source": str, "destination": str}, "error": str or None}
        """
        try:
            src = Path(source)
            dst = Path(destination)

            if not src.exists():
                return {"success": False, "data": None, "error": f"源文件不存在: {source}"}

            self._check_safe_path(dst)
            dst.parent.mkdir(parents=True, exist_ok=True)
            shutil.move(str(src), str(dst))

            bus = get_event_bus()
            await bus.publish("file.moved", {
                "source": str(src),
                "destination": str(dst),
            }, source="file_utils")

            return {
                "success": True,
                "data": {"source": str(src), "destination": str(dst)},
                "error": None,
            }
        except Exception as e:
            logger.error(f"移动文件失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def delete_file(self, filepath: str) -> Dict[str, Any]:
        """
        删除文件

        参数:
            filepath: 文件路径

        返回:
            {"success": bool, "data": {"filepath": str}, "error": str or None}
        """
        try:
            path = Path(filepath)

            if not path.exists():
                return {"success": False, "data": None, "error": f"文件不存在: {filepath}"}

            self._check_safe_path(path)
            path.unlink()

            bus = get_event_bus()
            await bus.publish("file.deleted", {
                "filepath": str(path),
            }, source="file_utils")

            return {
                "success": True,
                "data": {"filepath": str(path)},
                "error": None,
            }
        except Exception as e:
            logger.error(f"删除文件失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    async def read_file(self, filepath: str, max_lines: int = 100) -> Dict[str, Any]:
        """
        读取文件内容

        参数:
            filepath: 文件路径
            max_lines: 最大行数

        返回:
            {"success": bool, "data": {"filepath": str, "content": str, "lines": int}, "error": str or None}
        """
        try:
            path = Path(filepath)

            if not path.exists():
                return {"success": False, "data": None, "error": f"文件不存在: {filepath}"}

            content = path.read_text(encoding="utf-8")
            lines = content.splitlines()

            if len(lines) > max_lines:
                content = "\n".join(lines[:max_lines])
                content += f"\n... (共 {len(lines)} 行，已截断到前 {max_lines} 行)"

            return {
                "success": True,
                "data": {"filepath": str(path), "content": content, "lines": len(lines)},
                "error": None,
            }
        except Exception as e:
            logger.error(f"读取文件失败: {e}", exc_info=True)
            return {"success": False, "data": None, "error": str(e)}

    def _check_safe_path(self, path: Path):
        """检查路径是否安全"""
        resolved = str(path.resolve())
        for forbidden in FORBIDDEN_PATHS:
            if resolved == forbidden or resolved.startswith(forbidden + "/"):
                raise PermissionError(f"禁止操作受保护路径: {resolved}")
```

### 验证

```bash
python -m py_compile skills/file_utils/implementation.py
python scripts/validate_skills.py
```

### 提交

```bash
git add skills/file_utils/implementation.py
git commit -m "feat(file_utils): 实现 list_files / copy_file / move_file / delete_file / read_file"
```

---

## 第五步：Harness 检查

```bash
# 依赖方向
python scripts/check_dependencies.py

# 函数长度
python scripts/check_function_length.py

# 技能语法
python scripts/check_skills.py

# skill.json 格式
python scripts/validate_skills.py

# ruff
ruff check skills/web_search/ skills/file_utils/

# ruff 自动修复
ruff check skills/web_search/ skills/file_utils/ --fix

# pre-commit
pre-commit run --all-files
```

### 提交

```bash
git add -A
git commit -m "chore: web_search + file_utils Harness 检查通过"
```

---

## 第六步：端到端验证

### 6.1 技能加载

```bash
python -c "
from core.skill_registry import SkillRegistry
r = SkillRegistry()
r.scan()
total_tools = 0
for name, info in r.skills.items():
    tools = info['meta'].get('tools', [])
    total_tools += len(tools)
    print(f'{name}: {len(tools)} tools')
    for t in tools:
        print(f'  - {t[\"name\"]}')
print(f'总计: {len(r.skills)} skills, {total_tools} tools')
"
```

**预期**：
```
health_monitor: 1 tools
  - check_health
error_knowledge: 2 tools
  - store_error
  - classify_error
code_writer: 3 tools
  - write_code
  - read_code
  - modify_code
test_runner: 2 tools
  - run_tests
  - run_command
web_search: 2 tools
  - search
  - fetch_page
file_utils: 5 tools
  - list_files
  - copy_file
  - move_file
  - delete_file
  - read_file
总计: 6 skills, 15 tools
```

### 6.2 web_search 搜索测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('web_search', 'search', {'query': 'Python asyncio tutorial', 'max_results': 3})
    data = result.get('result', {}).get('data', {})
    for item in data.get('results', []):
        print(f'- {item[\"title\"]}')
        print(f'  {item[\"url\"]}')
        print()

asyncio.run(test())
"
```

**预期**：输出 3 条搜索结果（标题 + 链接）

### 6.3 file_utils 列表测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('file_utils', 'list_files', {'path': 'skills', 'pattern': '*'})
    data = result.get('result', {}).get('data', {})
    for d in data.get('dirs', []):
        print(f'[DIR]  {d[\"name\"]}')
    for f in data.get('files', []):
        print(f'[FILE] {f[\"name\"]} ({f[\"size\"]} bytes)')

asyncio.run(test())
"
```

**预期**：列出 skills/ 下的 6 个目录

### 6.4 file_utils 复制测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('file_utils', 'copy_file', {
        'source': 'skills/health_monitor/skill.json',
        'destination': 'data/health_monitor_backup.json'
    })
    print(result)

asyncio.run(test())
"
```

**预期**：`{"result": {"success": true, ...}}`

### 6.5 file_utils 读取测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('file_utils', 'read_file', {
        'filepath': 'data/health_monitor_backup.json',
        'max_lines': 10
    })
    print(result.get('result', {}).get('data', {}).get('content', ''))

asyncio.run(test())
"
```

### 6.6 file_utils 删除测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('file_utils', 'delete_file', {
        'filepath': 'data/health_monitor_backup.json'
    })
    print(result)

asyncio.run(test())
"
```

**预期**：`{"result": {"success": true, ...}}`

### 6.7 安全限制测试

```bash
python -c "
import asyncio
from core.skill_registry import SkillRegistry
from core.skill_handler import SkillHandler

async def test():
    r = SkillRegistry()
    r.scan()
    h = SkillHandler(r)
    result = await h.execute('file_utils', 'delete_file', {
        'filepath': '/etc/passwd'
    })
    print(result)

asyncio.run(test())
"
```

**预期**：`{"result": {"success": false, "error": "禁止操作受保护路径..."}}`

### 最终提交

```bash
git add -A
git commit -m "feat(skills): 添加 web_search 和 file_utils 技能（6 skills, 15 tools）"
git push
```

---

## 改动汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `skills/web_search/skill.json` | 新建 | MCP 格式，2 个工具 |
| `skills/web_search/implementation.py` | 新建 | search / fetch_page |
| `skills/file_utils/skill.json` | 新建 | MCP 格式，5 个工具 |
| `skills/file_utils/implementation.py` | 新建 | list_files / copy_file / move_file / delete_file / read_file |
| `requirements.txt` | 修改 | 添加 duckduckgo-search / beautifulsoup4 / requests |

## 技能清单（更新后）

| 技能 | 工具数 | 用途 |
|------|--------|------|
| health_monitor | 1 | 系统健康监控 |
| error_knowledge | 2 | 错误记录与分类 |
| code_writer | 3 | 写入、读取、修改代码 |
| test_runner | 2 | 运行测试、运行命令 |
| **web_search** | **2** | **网页搜索、获取网页内容** |
| **file_utils** | **5** | **文件列表、复制、移动、删除、读取** |
| **总计** | **6 skills, 15 tools** | |

## 安全机制

file_utils 内置安全限制：

```python
FORBIDDEN_PATHS = ["/", "C:\\", "/etc", "/usr", "/bin", "/sbin"]
```

禁止操作受保护的系统路径，防止 LLM 误删系统文件。

---

**从第一步开始执行，每步验证通过后再继续。**
```