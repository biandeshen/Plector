---
tags: [Plector, MCP, 协议]
type: feature
created: 2026-04-08
---

# Plector MCP Client 实现（Claude Code 可执行版）

> 本文档为增量更新，Plector v1.0 + 6 个技能已完成。
> 本次更新：实现 MCP Client，连接外部 MCP Server，引入现成工具。
>
> MCP（Model Context Protocol）是 Anthropic 提出的标准协议：
> - MCP Server：暴露工具（如 GitHub / Filesystem / 搜索）
> - MCP Client：发现工具、调用工具
> - 通信方式：JSON-RPC 2.0 over stdio / HTTP+SSE
>
> 共 8 步，按顺序执行。

---

## 第一步：创建配置文件

更新 `config/config.yaml`，在末尾添加 MCP 配置：

```yaml
llm:
  provider: "${LLM_PROVIDER:-ollama}"
  max_iterations: 10

  ollama:
    base_url: "http://localhost:11434"
    model: "${OLLAMA_MODEL:-qwen3:4b}"

  openai:
    api_key: "${OPENAI_API_KEY}"
    model: "${OPENAI_MODEL:-gpt-4o-mini}"
    base_url: "${OPENAI_BASE_URL:-https://api.openai.com/v1}"

  anthropic:
    api_key: "${ANTHROPIC_API_KEY}"
    model: "${ANTHROPIC_MODEL:-claude-sonnet-4-20250514}"

# MCP Server 配置
mcp:
  servers:
    # 文件系统 MCP Server（本地 stdio）
    filesystem:
      enabled: true
      transport: "stdio"
      command: "npx"
      args: ["-y", "@modelcontextprotocol/server-filesystem", "."]
      description: "文件系统操作（读写、搜索、目录管理）"

    # GitHub MCP Server（本地 stdio）
    github:
      enabled: false
      transport: "stdio"
      command: "npx"
      args: ["-y", "@modelcontextprotocol/server-github"]
      env:
        GITHUB_TOKEN: "${GITHUB_TOKEN}"
      description: "GitHub 操作（Issues / PR / Repos）"

    # 自定义 MCP Server（HTTP + SSE）
    # custom:
    #   enabled: false
    #   transport: "http"
    #   url: "http://localhost:3000/sse"
    #   description: "自定义 MCP Server"
```

### 验证

```bash
python -c "
import yaml
with open('config/config.yaml') as f:
    config = yaml.safe_load(f)
servers = config.get('mcp', {}).get('servers', {})
for name, server in servers.items():
    enabled = server.get('enabled', False)
    print(f'{name}: enabled={enabled}, transport={server.get(\"transport\")}')
"
```

### 提交

```bash
git add config/config.yaml
git commit -m "feat(config): 添加 MCP Server 配置（filesystem / github）"
```

---

## 第二步：创建 MCP Client `core/mcp_client.py`

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
MCP Client - 连接外部 MCP Server，发现并调用工具

功能：
    1. 通过 stdio 或 HTTP+SSE 连接 MCP Server
    2. 发现远程工具（tools/list）
    3. 调用远程工具（tools/call）
    4. 将远程工具注册到 ToolRegistry

通信协议：JSON-RPC 2.0

Author: Plector
Version: 1.0.0
Created: 2026-04-04
"""

import asyncio
import json
import logging
import os
from typing import Any, Callable, Dict, List, Optional

logger = logging.getLogger(__name__)


class MCPServer:
    """单个 MCP Server 连接"""

    def __init__(self, name: str, config: dict):
        self.name = name
        self.config = config
        self.transport = config.get("transport", "stdio")
        self.description = config.get("description", "")
        self.process = None
        self._request_id = 0
        self._connected = False

    async def connect(self):
        """连接 MCP Server"""
        if self.transport == "stdio":
            await self._connect_stdio()
        elif self.transport == "http":
            await self._connect_http()
        else:
            raise ValueError(f"不支持的 transport: {self.transport}")

    async def _connect_stdio(self):
        """通过 stdio 连接"""
        command = self.config.get("command")
        args = self.config.get("args", [])
        env_config = self.config.get("env", {})

        # 解析环境变量
        env = {**os.environ}
        for key, value in env_config.items():
            if isinstance(value, str) and value.startswith("${") and value.endswith("}"):
                env_name = value[2:-1]
                env[key] = os.environ.get(env_name, "")
            else:
                env[key] = str(value)

        try:
            self.process = await asyncio.create_subprocess_exec(
                command, *args,
                stdin=asyncio.subprocess.PIPE,
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
                env=env,
            )
            self._connected = True
            logger.info(f"MCP Server '{self.name}' 已连接（stdio）")

            # 发送 initialize 请求
            await self._send_request("initialize", {
                "protocolVersion": "2024-11-05",
                "capabilities": {"roots": {"listChanged": True}},
                "clientInfo": {"name": "Plector", "version": "1.0.0"},
            })
        except FileNotFoundError:
            logger.error(f"MCP Server '{self.name}' 启动失败：命令 '{command}' 不存在")
            self._connected = False
        except Exception as e:
            logger.error(f"MCP Server '{self.name}' 连接失败: {e}")
            self._connected = False

    async def _connect_http(self):
        """通过 HTTP+SSE 连接"""
        # TODO: 实现 HTTP+SSE 传输
        logger.warning(f"MCP Server '{self.name}' HTTP 传输尚未实现")
        self._connected = False

    async def list_tools(self) -> List[Dict]:
        """发现远程工具"""
        if not self._connected:
            return []
        try:
            response = await self._send_request("tools/list", {})
            return response.get("result", {}).get("tools", [])
        except Exception as e:
            logger.error(f"获取 MCP Server '{self.name}' 工具列表失败: {e}")
            return []

    async def call_tool(self, tool_name: str, arguments: dict) -> dict:
        """调用远程工具"""
        if not self._connected:
            return {"jsonrpc": "2.0", "error": {"code": -32603, "message": f"MCP Server '{self.name}' 未连接"}}
        try:
            response = await self._send_request("tools/call", {
                "name": tool_name,
                "arguments": arguments,
            })
            return response
        except Exception as e:
            return {"jsonrpc": "2.0", "error": {"code": -32603, "message": str(e)}}

    async def _send_request(self, method: str, params: dict) -> dict:
        """发送 JSON-RPC 2.0 请求"""
        self._request_id += 1
        request = {
            "jsonrpc": "2.0",
            "id": self._request_id,
            "method": method,
            "params": params,
        }
        request_line = json.dumps(request) + "\n"

        self.process.stdin.write(request_line.encode("utf-8"))
        await self.process.stdin.drain()

        # 读取响应
        response_line = await self.process.stdout.readline()
        if not response_line:
            raise ConnectionError(f"MCP Server '{self.name}' 无响应")

        response = json.loads(response_line.decode("utf-8"))

        # 跳过通知（无 id 的消息）
        while "id" not in response:
            response_line = await self.process.stdout.readline()
            if not response_line:
                raise ConnectionError(f"MCP Server '{self.name}' 无响应")
            response = json.loads(response_line.decode("utf-8"))

        return response

    async def disconnect(self):
        """断开连接"""
        if self.process:
            self.process.terminate()
            await self.process.wait()
            self._connected = False
            logger.info(f"MCP Server '{self.name}' 已断开")


class MCPClient:
    """MCP Client，管理多个 MCP Server 连接"""

    def __init__(self, config: dict):
        self.servers: Dict[str, MCPServer] = {}
        self.server_config = config.get("mcp", {}).get("servers", {})

    async def connect_all(self):
        """连接所有 enabled 的 MCP Server"""
        for name, server_config in self.server_config.items():
            if not server_config.get("enabled", False):
                logger.info(f"MCP Server '{name}' 未启用，跳过")
                continue

            server = MCPServer(name, server_config)
            await server.connect()
            if server._connected:
                self.servers[name] = server

    async def list_all_tools(self) -> Dict[str, List[Dict]]:
        """获取所有 MCP Server 的工具列表"""
        all_tools = {}
        for name, server in self.servers.items():
            tools = await server.list_tools()
            all_tools[name] = tools
        return all_tools

    async def call_tool(self, server_name: str, tool_name: str, arguments: dict) -> dict:
        """调用指定 MCP Server 的工具"""
        server = self.servers.get(server_name)
        if not server:
            return {"jsonrpc": "2.0", "error": {"code": -32601, "message": f"MCP Server '{server_name}' 未连接"}}
        return await server.call_tool(tool_name, arguments)

    def register_to_tool_registry(self, tool_registry, all_tools: Dict[str, List[Dict]]):
        """将远程工具注册到 ToolRegistry"""
        for server_name, tools in all_tools.items():
            for tool in tools:
                remote_name = f"mcp_{server_name}_{tool['name']}"
                tool_registry.register(
                    name=remote_name,
                    description=f"[MCP:{server_name}] {tool.get('description', '')}",
                    input_schema=tool.get("inputSchema", {
                        "type": "object",
                        "properties": {},
                        "required": [],
                        "additionalProperties": False,
                    }),
                    handler=self._create_handler(server_name, tool["name"]),
                )
                logger.info(f"注册远程工具: {remote_name}")

    def _create_handler(self, server_name: str, tool_name: str) -> Callable:
        """创建远程工具的处理函数"""
        async def handler(**kwargs):
            result = await self.call_tool(server_name, tool_name, kwargs)
            # 将 MCP 结果转换为 Plector 格式
            if "error" in result:
                return {"success": False, "data": None, "error": result["error"].get("message", "未知错误")}
            mcp_result = result.get("result", {})
            content = mcp_result.get("content", [])
            # 提取文本内容
            text_parts = []
            for item in content:
                if item.get("type") == "text":
                    text_parts.append(item.get("text", ""))
            return {
                "success": True,
                "data": {"text": "\n".join(text_parts), "raw": mcp_result},
                "error": None,
            }
        return handler

    async def disconnect_all(self):
        """断开所有 MCP Server 连接"""
        for name, server in self.servers.items():
            await server.disconnect()
        self.servers.clear()
```

### 验证

```bash
python -c "from core.mcp_client import MCPClient; print('OK')"
```

### 提交

```bash
git add core/mcp_client.py
git commit -m "feat(core): 添加 MCP Client（stdio 传输 + JSON-RPC 2.0 + 工具注册）"
```

---

## 第三步：集成 MCP Client 到 AgentLoop

修改 `core/agent_loop.py`：

```python
import asyncio
import json
from .skill_registry import SkillRegistry
from .skill_handler import SkillHandler
from .function_calling import ToolRegistry
from .event_bus import get_event_bus
from .closure_engine import ClosureEngine
from .context_builder import ContextBuilder
from .llm_client import LLMClient
from .mcp_client import MCPClient


class AgentLoop:
    """自主决策循环，实现 ReAct 模式"""

    def __init__(self, config: dict = None):
        self.config = config or {}
        self.skill_registry = SkillRegistry()
        self.skill_registry.scan()
        self.skill_handler = SkillHandler(self.skill_registry)
        self.tool_registry = ToolRegistry()
        self.event_bus = get_event_bus()
        self.context_builder = ContextBuilder(self.skill_registry)
        self.closure_engine = ClosureEngine(self.skill_handler)
        self.max_iterations = self.config.get("max_iterations", 10)
        self.llm = LLMClient(self.config.get("llm", {}))
        self.mcp_client = MCPClient(self.config)
        self._register_skills_as_tools()
        self._register_mcp_tools()

    def _register_skills_as_tools(self):
        """将每个技能的工具注册为 LLM 可调用工具（MCP 格式）"""
        for skill_name, skill_info in self.skill_registry.skills.items():
            for tool_def in skill_info["meta"].get("tools", []):
                tool_name = f"{skill_name}_{tool_def['name']}"
                self.tool_registry.register(
                    name=tool_name,
                    description=tool_def.get("description", ""),
                    input_schema=tool_def.get("inputSchema", {
                        "type": "object",
                        "properties": {},
                        "required": [],
                        "additionalProperties": False,
                    }),
                    handler=self._create_skill_handler(skill_name, tool_def["name"])
                )

    def _register_mcp_tools(self):
        """连接 MCP Server 并注册远程工具"""
        try:
            asyncio.get_event_loop().run_until_complete(self._init_mcp())
        except RuntimeError:
            # 如果已经在事件循环中，跳过
            pass
        except Exception as e:
            logger.warning(f"MCP Client 初始化失败: {e}")

    async def _init_mcp(self):
        """异步初始化 MCP Client"""
        await self.mcp_client.connect_all()
        all_tools = await self.mcp_client.list_all_tools()
        self.mcp_client.register_to_tool_registry(self.tool_registry, all_tools)

    def _create_skill_handler(self, skill_name, method_name):
        async def handler(**kwargs):
            return await self.skill_handler.execute(skill_name, method_name, kwargs)
        return handler

    async def run(self, user_input: str, session_id: str = None) -> str:
        """执行 Agent 循环"""
        system_prompt = self.context_builder.build_system_prompt()
        messages = [
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_input}
        ]

        for _ in range(self.max_iterations):
            response = await self.llm.chat(
                messages=messages,
                tools=self.tool_registry.get_tool_schemas()
            )
            if not response.get("tool_calls"):
                return response["content"]

            for tool_call in response["tool_calls"]:
                result = await self.tool_registry.execute(tool_call)
                messages.append({
                    "role": "tool",
                    "tool_call_id": tool_call.get("id"),
                    "content": json.dumps(result)
                })

        return "达到最大迭代次数"
```

### 但有一个问题

`_register_mcp_tools` 在 `__init__` 中调用，但 MCP Server 启动需要时间，可能会阻塞。

**更好的方案**：不在 `__init__` 中初始化 MCP，而是在首次调用 `run()` 时懒加载。

修改 `_init_mcp` 和 `run` 方法：

```python
    def __init__(self, config: dict = None):
        self.config = config or {}
        self.skill_registry = SkillRegistry()
        self.skill_registry.scan()
        self.skill_handler = SkillHandler(self.skill_registry)
        self.tool_registry = ToolRegistry()
        self.event_bus = get_event_bus()
        self.context_builder = ContextBuilder(self.skill_registry)
        self.closure_engine = ClosureEngine(self.skill_handler)
        self.max_iterations = self.config.get("max_iterations", 10)
        self.llm = LLMClient(self.config.get("llm", {}))
        self.mcp_client = MCPClient(self.config)
        self._mcp_initialized = False
        self._register_skills_as_tools()

    async def _ensure_mcp_initialized(self):
        """懒加载 MCP 工具"""
        if self._mcp_initialized:
            return
        try:
            await self.mcp_client.connect_all()
            all_tools = await self.mcp_client.list_all_tools()
            self.mcp_client.register_to_tool_registry(self.tool_registry, all_tools)
            self._mcp_initialized = True
        except Exception as e:
            logger.warning(f"MCP Client 初始化失败: {e}")
            self._mcp_initialized = True  # 标记为已初始化，避免重复尝试

    async def run(self, user_input: str, session_id: str = None) -> str:
        """执行 Agent 循环"""
        # 懒加载 MCP 工具
        await self._ensure_mcp_initialized()

        system_prompt = self.context_builder.build_system_prompt()
        messages = [
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_input}
        ]

        for _ in range(self.max_iterations):
            response = await self.llm.chat(
                messages=messages,
                tools=self.tool_registry.get_tool_schemas()
            )
            if not response.get("tool_calls"):
                return response["content"]

            for tool_call in response["tool_calls"]:
                result = await self.tool_registry.execute(tool_call)
                messages.append({
                    "role": "tool",
                    "tool_call_id": tool_call.get("id"),
                    "content": json.dumps(result)
                })

        return "达到最大迭代次数"
```

### 验证

```bash
python -c "from core.agent_loop import AgentLoop; a = AgentLoop(); print('OK')"
```

### 提交

```bash
git add core/agent_loop.py
git commit -m "feat(core): AgentLoop 集成 MCP Client（懒加载 + 远程工具注册）"
```

---

## 第四步：安装 MCP Server（filesystem）

```bash
# 安装 Node.js（如果没有）
# https://nodejs.org

# 安装 filesystem MCP Server
npm install -g @modelcontextprotocol/server-filesystem

# 验证
npx @modelcontextprotocol/server-filesystem --help
```

### 如果没有 Node.js

可以用 Python 版的 MCP Server：

```bash
pip install mcp-server-filesystem
```

然后更新 `config/config.yaml`：

```yaml
mcp:
  servers:
    filesystem:
      enabled: true
      transport: "stdio"
      command: "python"
      args: ["-m", "mcp_server_filesystem", "."]
      description: "文件系统操作"
```

### 验证

```bash
python -c "
import asyncio
from core.mcp_client import MCPServer

async def test():
    server = MCPServer('test', {
        'transport': 'stdio',
        'command': 'npx',
        'args': ['-y', '@modelcontextprotocol/server-filesystem', '.'],
    })
    await server.connect()
    if server._connected:
        tools = await server.list_tools()
        print(f'发现 {len(tools)} 个远程工具:')
        for t in tools:
            print(f'  - {t[\"name\"]}: {t.get(\"description\", \"\")}')
        await server.disconnect()
    else:
        print('连接失败')

asyncio.run(test())
"
```

**预期**：
```
发现 N 个远程工具:
  - read_file: 读取文件
  - write_file: 写入文件
  - list_directory: 列出目录
  - search_files: 搜索文件
  - move_file: 移动文件
  - get_file_info: 获取文件信息
  ...
```

### 提交

```bash
git add -A
git commit -m "feat(mcp): 安装并验证 filesystem MCP Server"
```

---

## 第五步：Harness 检查

```bash
# 依赖方向
python scripts/check_dependencies.py

# 函数长度
python scripts/check_function_length.py

# 技能语法
python scripts/check_skills.py

# skill.json 格式
python scripts/validate_skills.py

# ruff
ruff check core/mcp_client.py
ruff check core/mcp_client.py --fix

# pre-commit
pre-commit run --all-files
```

### 提交

```bash
git add -A
git commit -m "chore: MCP Client Harness 检查通过"
```

---

## 第六步：端到端验证

### 6.1 MCP Client 模块导入

```bash
python -c "
from core.mcp_client import MCPClient, MCPServer
print('MCPClient: OK')
print('MCPServer: OK')
"
```

### 6.2 AgentLoop 工具注册（含 MCP 工具）

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    agent = AgentLoop()
    # 触发 MCP 初始化
    await agent._ensure_mcp_initialized()
    schemas = agent.tool_registry.get_tool_schemas()
    local_count = 0
    mcp_count = 0
    for s in schemas:
        name = s['function']['name']
        if name.startswith('mcp_'):
            mcp_count += 1
            print(f'  [MCP] {name}')
        else:
            local_count += 1
    print(f'本地工具: {local_count}')
    print(f'MCP 工具: {mcp_count}')
    print(f'总计: {len(schemas)}')

asyncio.run(test())
"
```

**预期**：
```
  [MCP] mcp_filesystem_read_file
  [MCP] mcp_filesystem_write_file
  [MCP] mcp_filesystem_list_directory
  ...
本地工具: 15
MCP 工具: N
总计: 15 + N
```

### 6.3 MCP 工具调用测试

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    agent = AgentLoop()
    await agent._ensure_mcp_initialized()
    # 调用 MCP filesystem 的 list_directory
    result = await agent.tool_registry.execute({
        'function': {
            'name': 'mcp_filesystem_list_directory',
            'arguments': '{\"path\": \".\"}'
        }
    })
    print(result)

asyncio.run(test())
"
```

### 6.4 CLI 端到端测试

```bash
python channels/cli.py --query "列出当前目录下的文件"
```

**预期**：LLM 调用 MCP 工具（或本地 file_utils），返回文件列表

### 6.5 关闭 MCP Server

```bash
python -c "
import asyncio
from core.agent_loop import AgentLoop

async def test():
    agent = AgentLoop()
    await agent._ensure_mcp_initialized()
    await agent.mcp_client.disconnect_all()
    print('MCP Server 已断开')

asyncio.run(test())
"
```

### 最终提交

```bash
git add -A
git commit -m "feat(mcp): MCP Client 完整实现（连接 / 发现 / 调用 / 注册远程工具）"
git push
```

---

## 第七步：更新 CLAUDE.md

在 `## 项目结构` 中添加：

```markdown
├── core/
│   ├── mcp_client.py       # MCP Client（连接外部 MCP Server）
```

在 `## 详细规范` 中添加：

```markdown
- MCP 协议：`config/config.yaml` 中的 `mcp.servers` 配置
```

### 提交

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md 添加 MCP Client 说明"
```

---

## 第八步：更新 requirements.txt

```txt
psutil>=5.9.0
pyyaml>=6.0
pytest>=7.0
pytest-asyncio>=0.21
pre-commit>=3.0
ollama>=0.1.0
openai>=1.0.0
anthropic>=0.20.0
ruff>=0.4.0
mypy>=1.10.0
beautifulsoup4>=4.12.0
requests>=2.31.0
```

### 提交

```bash
git add requirements.txt
git commit -m "chore: 更新 requirements.txt"
```

---

## 改动汇总

| 文件 | 操作 | 说明 |
|------|------|------|
| `core/mcp_client.py` | 新建 | MCP Client（MCPServer + MCPClient） |
| `core/agent_loop.py` | 修改 | 集成 MCP Client（懒加载） |
| `config/config.yaml` | 修改 | 添加 MCP Server 配置 |
| `CLAUDE.md` | 修改 | 添加 MCP 说明 |
| `requirements.txt` | 修改 | 依赖更新 |

## 架构变化

```
改动前：
  AgentLoop → ToolRegistry → 本地技能

改动后：
  AgentLoop → ToolRegistry → 本地技能
                            → MCP Server 1（filesystem）
                            → MCP Server 2（github）
                            → MCP Server N（...）
```

## MCP 生态可用的 Server

| MCP Server | 功能 | 安装 |
|-----------|------|------|
| filesystem | 文件操作 | `npx -y @modelcontextprotocol/server-filesystem` |
| github | GitHub 操作 | `npx -y @modelcontextprotocol/server-github` |
| sqlite | SQLite 数据库 | `npx -y @modelcontextprotocol/server-sqlite` |
| puppeteer | 浏览器自动化 | `npx -y @modelcontextprotocol/server-puppeteer` |
| fetch | HTTP 请求 | `npx -y @modelcontextprotocol/server-fetch` |
| memory | 知识图谱 | `npx -y @modelcontextprotocol/server-memory` |
| slack | Slack 集成 | `npx -y @modelcontextprotocol/server-slack` |
| postgres | PostgreSQL | `npx -y @modelcontextprotocol/server-postgres` |

使用方式：在 `config/config.yaml` 中添加对应的 server 配置，设置 `enabled: true`。

---

**从第一步开始执行，每步验证通过后再继续。**
```