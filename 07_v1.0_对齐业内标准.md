---
tags: [Plector, 规范, 设计]
type: spec
created: 2026-04-08
---

# Plector 对齐业内标准（Claude Code 可执行版）

> 本文档为增量更新，Plector 核心模块已完成。
> 本次更新：将私有格式替换为业内标准格式。
>
> 对齐标准：
> - 技能定义 → MCP Tool 格式
> - 工具 Schema → OpenAI Function Calling 格式
> - 事件格式 → CloudEvents 格式
> - 错误格式 → JSON-RPC 2.0 格式
>
> 改动文件：6 个修改 + 1 个新建

---

## 第一步：修改 skill.json（MCP Tool 格式）

### 1.1 skills/health_monitor/skill.json

替换整个文件：

```json
{
  "name": "health_monitor",
  "description": "获取系统健康状态，包括 CPU、内存、磁盘使用率",
  "version": "1.0.0",
  "tier": "tier_1_system",
  "dependencies": [],
  "events_produced": ["health.degraded", "health.recovered"],
  "events_consumed": [],
  "tools": [
    {
      "name": "check_health",
      "description": "执行健康检查，返回 CPU、内存、磁盘使用率和健康状态",
      "inputSchema": {
        "type": "object",
        "properties": {},
        "required": [],
        "additionalProperties": false
      }
    }
  ]
}
```

### 1.2 skills/error_knowledge/skill.json

替换整个文件：

```json
{
  "name": "error_knowledge",
  "description": "记录错误并分类，存储到本地知识库",
  "version": "1.0.0",
  "tier": "tier_2_functional",
  "dependencies": [],
  "events_produced": ["error.classified", "error.stored"],
  "events_consumed": ["test.failed", "skill.failed"],
  "tools": [
    {
      "name": "store_error",
      "description": "存储错误信息到本地知识库",
      "inputSchema": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string",
            "description": "错误描述"
          }
        },
        "required": ["error"],
        "additionalProperties": false
      }
    },
    {
      "name": "classify_error",
      "description": "分类错误类型，返回分类结果和置信度",
      "inputSchema": {
        "type": "object",
        "properties": {
          "error": {
            "type": "string",
            "description": "错误描述"
          }
        },
        "required": ["error"],
        "additionalProperties": false
      }
    }
  ]
}
```

### 验证

```bash
python -c "from core.skill_registry import SkillRegistry; r = SkillRegistry(); r.scan(); print(f'{len(r.skills)} skills loaded')"
# 应输出: 2 skills loaded
```

### 提交

```bash
git add skills/*/skill.json
git commit -m "refactor(skills): skill.json 改为 MCP Tool 格式（methods→tools, params→inputSchema）"
```

---

## 第二步：修改 AgentLoop（读 tools 而非 methods）

### 2.1 修改 core/agent_loop.py

找到 `_register_skills_as_tools` 方法，替换为：

```python
    def _register_skills_as_tools(self):
        """将每个技能的工具注册为 LLM 可调用工具（MCP 格式）"""
        for skill_name, skill_info in self.skill_registry.skills.items():
            for tool_def in skill_info["meta"].get("tools", []):
                tool_name = f"{skill_name}.{tool_def['name']}"
                self.tool_registry.register(
                    name=tool_name,
                    description=tool_def.get("description", ""),
                    input_schema=tool_def.get("inputSchema", {
                        "type": "object",
                        "properties": {},
                        "required": [],
                        "additionalProperties": False,
                    }),
                    handler=self._create_skill_handler(skill_name, tool_def["name"])
                )
```

### 验证

```bash
python -c "from core.agent_loop import AgentLoop; a = AgentLoop(); print(f'{len(a.tool_registry.get_tool_schemas())} tools registered')"
# 应输出: 3 tools registered（check_health + store_error + classify_error）
```

### 提交

```bash
git add core/agent_loop.py
git commit -m "refactor(core): AgentLoop 改为读取 MCP 格式的 tools 字段"
```

---

## 第三步：修改 ToolRegistry（对齐 OpenAI Function Calling）

### 3.1 修改 core/function_calling.py

替换整个文件：

```python
import json
import asyncio
from typing import Callable, Dict


class ToolRegistry:
    """工具注册表，输出 OpenAI Function Calling 格式"""

    def __init__(self):
        self._tools: Dict[str, Dict] = {}

    def register(self, name: str, description: str, input_schema: dict, handler: Callable):
        """
        注册工具

        参数:
            name: 工具名称（如 health_monitor.check_health）
            description: 工具描述
            input_schema: JSON Schema 格式的参数定义
            handler: 异步或同步的处理函数
        """
        # 确保 input_schema 是完整 JSON Schema
        if "type" not in input_schema:
            input_schema = {
                "type": "object",
                "properties": input_schema,
                "required": list(input_schema.keys()),
                "additionalProperties": False,
            }
        # 确保 additionalProperties 存在
        if "additionalProperties" not in input_schema:
            input_schema["additionalProperties"] = False

        self._tools[name] = {
            "handler": handler,
            "schema": {
                "type": "function",
                "function": {
                    "name": name,
                    "description": description,
                    "parameters": input_schema,
                    "strict": True,
                }
            }
        }

    def get_tool_schemas(self) -> list:
        """获取所有工具的 OpenAI Function Calling Schema"""
        return [info["schema"] for info in self._tools.values()]

    async def execute(self, tool_call: dict) -> dict:
        """
        执行工具调用

        参数:
            tool_call: LLM 返回的 tool_call 对象
                OpenAI 格式: {"function": {"name": "...", "arguments": "..."}, "id": "..."}
                Ollama 格式: {"function": {"name": "...", "arguments": {...}}}

        返回:
            {"success": bool, "data": any, "error": str or None}
        """
        name = tool_call["function"]["name"]
        arguments = tool_call["function"]["arguments"]
        # 兼容两种格式：OpenAI 返回字符串，Ollama 返回 dict
        if isinstance(arguments, str):
            arguments = json.loads(arguments)

        tool = self._tools.get(name)
        if not tool:
            return {"success": False, "data": None, "error": f"工具 {name} 不存在"}

        try:
            result = tool["handler"](**arguments)
            if asyncio.iscoroutine(result):
                result = await result
            return result if isinstance(result, dict) else {"success": True, "data": result, "error": None}
        except Exception as e:
            return {"success": False, "data": None, "error": str(e)}
```

### 验证

```bash
python -c "
from core.function_calling import ToolRegistry
t = ToolRegistry()
t.register('test', '测试', {'msg': {'type': 'string', 'description': '消息'}}, lambda msg: msg)
schema = t.get_tool_schemas()[0]
assert schema['type'] == 'function'
assert schema['function']['strict'] == True
assert schema['function']['parameters']['additionalProperties'] == False
print('OK: OpenAI Function Calling 格式验证通过')
"
```

### 提交

```bash
git add core/function_calling.py
git commit -m "refactor(core): ToolRegistry 对齐 OpenAI Function Calling 格式（strict + additionalProperties）"
```

---

## 第四步：修改事件格式（CloudEvents）

### 4.1 修改 core/event_bus.py

替换整个文件：

```python
import asyncio
import time
import uuid
from collections import defaultdict
from typing import Callable, Dict, List


class EventBus:
    """
    异步事件总线，对齐 CloudEvents 格式

    CloudEvents 格式：
    {
        "specversion": "1.0",
        "id": "唯一ID",
        "source": "发布者",
        "type": "event.type",
        "time": "ISO 8601",
        "data": {...}
    }
    """

    def __init__(self):
        self._subscribers: Dict[str, List[Callable]] = defaultdict(list)

    def subscribe(self, event_type: str, handler: Callable):
        """注册事件处理器，支持通配符 'skill.*'"""
        self._subscribers[event_type].append(handler)

    async def publish(self, event_type: str, data: dict, source: str = "plector"):
        """
        发布 CloudEvents 格式的事件

        参数:
            event_type: 事件类型，如 "health.degraded"
            data: 事件数据
            source: 发布者名称
        """
        event = {
            "specversion": "1.0",
            "id": str(uuid.uuid4()),
            "source": source,
            "type": event_type,
            "time": time.strftime("%Y-%m-%dT%H:%M:%SZ", time.gmtime()),
            "data": data,
        }
        # 精确匹配
        for handler in self._subscribers.get(event_type, []):
            asyncio.create_task(handler(event))
        # 通配符匹配
        for pattern in list(self._subscribers.keys()):
            if pattern.endswith('*') and event_type.startswith(pattern[:-1]):
                for handler in self._subscribers[pattern]:
                    asyncio.create_task(handler(event))


# 全局单例
_instance = None

def get_event_bus() -> EventBus:
    """获取全局 EventBus 单例"""
    global _instance
    if _instance is None:
        _instance = EventBus()
    return _instance
```

### 4.2 更新事件订阅者

**注意**：事件格式从 `{"msg": "hello"}` 变为 `{"specversion": "1.0", "data": {"msg": "hello"}, ...}`

需要更新所有事件订阅者的 payload 解析。修改 `skills/error_knowledge/implementation.py` 中的事件处理：

```python
    async def _on_test_failed(self, event: dict):
        """处理 test.failed 事件（CloudEvents 格式）"""
        data = event.get("data", {})
        error = data.get("error", "unknown error")
        await self.store_error(error=error)

    async def _on_skill_failed(self, event: dict):
        """处理 skill.failed 事件（CloudEvents 格式）"""
        data = event.get("data", {})
        error = data.get("error", "unknown error")
        await self.store_error(error=error)
```

修改 `skills/health_monitor/implementation.py` 中的事件发布：

```python
            # 发布 CloudEvents 格式的健康事件
            bus = get_event_bus()
            await bus.publish(f"health.{status}", {
                "cpu": cpu,
                "memory": memory,
                "disk": disk,
            }, source="health_monitor")
```

### 验证

```bash
python -c "
import asyncio
from core.event_bus import EventBus

async def test():
    bus = EventBus()
    received = []
    async def handler(event):
        received.append(event)
    bus.subscribe('test.event', handler)
    await bus.publish('test.event', {'msg': 'hello'}, source='test')
    await asyncio.sleep(0.1)
    assert received[0]['specversion'] == '1.0'
    assert received[0]['type'] == 'test.event'
    assert received[0]['data']['msg'] == 'hello'
    print('OK: CloudEvents 格式验证通过')

asyncio.run(test())
"
```

### 提交

```bash
git add core/event_bus.py skills/error_knowledge/implementation.py skills/health_monitor/implementation.py
git commit -m "refactor(core): 事件格式对齐 CloudEvents（specversion/id/source/type/time/data）"
```

---

## 第五步：修改错误格式（JSON-RPC 2.0）

### 5.1 修改 core/function_calling.py 中的错误返回

找到 `execute` 方法中的错误返回，替换为：

```python
    async def execute(self, tool_call: dict) -> dict:
        """执行工具调用，返回 JSON-RPC 2.0 格式"""
        name = tool_call["function"]["name"]
        arguments = tool_call["function"]["arguments"]
        if isinstance(arguments, str):
            arguments = json.loads(arguments)

        tool = self._tools.get(name)
        if not tool:
            return {
                "jsonrpc": "2.0",
                "error": {"code": -32601, "message": f"工具 {name} 不存在"}
            }

        try:
            result = tool["handler"](**arguments)
            if asyncio.iscoroutine(result):
                result = await result
            return {
                "jsonrpc": "2.0",
                "result": result if isinstance(result, dict) else {"data": result}
            }
        except Exception as e:
            return {
                "jsonrpc": "2.0",
                "error": {"code": -32603, "message": str(e)}
            }
```

### 5.2 JSON-RPC 2.0 错误码

| code | 含义 | 触发条件 |
|------|------|---------|
| -32700 | Parse error | JSON 解析失败 |
| -32600 | Invalid Request | 请求格式错误 |
| -32601 | Method not found | 工具/方法不存在 |
| -32602 | Invalid params | 参数错误 |
| -32603 | Internal error | 执行异常 |

### 验证

```bash
python -c "
import asyncio
from core.function_calling import ToolRegistry

async def test():
    t = ToolRegistry()
    result = await t.execute({'function': {'name': 'nonexistent', 'arguments': '{}'}})
    assert result.get('jsonrpc') == '2.0'
    assert result.get('error', {}).get('code') == -32601
    print('OK: JSON-RPC 2.0 错误格式验证通过')

asyncio.run(test())
"
```

### 提交

```bash
git add core/function_calling.py
git commit -m "refactor(core): 错误格式对齐 JSON-RPC 2.0（error.code + error.message）"
```

---

## 第六步：修改 validate_skills.py（校验 MCP 格式）

替换 `scripts/validate_skills.py`：

```python
#!/usr/bin/env python3
"""
校验 skill.json 是否符合 MCP Tool 格式规范
"""
import json
import sys
from pathlib import Path

REQUIRED_FIELDS = [
    "name", "description", "version", "tier",
    "dependencies", "events_produced", "events_consumed", "tools"
]

VALID_TIERS = ["tier_0_kernel", "tier_1_system", "tier_2_functional", "tier_3_tool"]

REQUIRED_TOOL_FIELDS = ["name", "description", "inputSchema"]

REQUIRED_SCHEMA_FIELDS = ["type", "properties", "required", "additionalProperties"]


def validate_skill(data, filepath):
    """校验单个 skill.json"""
    errors = []

    # 校验顶层字段
    for field in REQUIRED_FIELDS:
        if field not in data:
            errors.append(f"[{filepath}] Missing '{field}'")

    # 校验 tier
    if data.get("tier") not in VALID_TIERS:
        errors.append(f"[{filepath}] Invalid tier '{data.get('tier')}'")

    # 校验 tools（MCP 格式）
    for tool in data.get("tools", []):
        for field in REQUIRED_TOOL_FIELDS:
            if field not in tool:
                errors.append(f"[{filepath}] Missing tool field '{field}' in tool '{tool.get('name', '?')}'")

        # 校验 inputSchema（JSON Schema）
        schema = tool.get("inputSchema", {})
        for field in REQUIRED_SCHEMA_FIELDS:
            if field not in schema:
                errors.append(f"[{filepath}] Missing inputSchema field '{field}' in tool '{tool.get('name', '?')}'")

    return errors


def main():
    errors = 0
    for skill_json in Path("skills").rglob("skill.json"):
        with open(skill_json, encoding="utf-8") as f:
            data = json.load(f)
        skill_errors = validate_skill(data, skill_json)
        for err in skill_errors:
            print(err)
        errors += len(skill_errors)
    sys.exit(errors)


if __name__ == "__main__":
    main()
```

### 验证

```bash
python scripts/validate_skills.py
# 应无输出（exit code 0）
```

### 提交

```bash
git add scripts/validate_skills.py
git commit -m "refactor(scripts): validate_skills.py 校验 MCP Tool 格式"
```

---

## 第七步：更新 CLAUDE.md

在 CLAUDE.md 中找到 `## 技能开发` 部分，替换为：

```markdown
## 技能开发

- 目录：`skills/<name>/`，含 `skill.json` + `implementation.py`
- 类名：`SkillHandler`
- 方法名：与 `skill.json` 中 `tools` 的 `name` 一致
- 参数定义：`tools[].inputSchema`（JSON Schema）
- 返回格式：`{"success": bool, "data": any, "error": str or None}`
- 事件：CloudEvents 格式，用 `get_event_bus()` 发布/订阅
- 错误：JSON-RPC 2.0 格式
- 创建后：`python -m py_compile skills/<name>/implementation.py`
- 验证：`python scripts/validate_skills.py`
```

### 提交

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md 更新为 MCP/CloudEvents/JSON-RPC 2.0 规范"
```

---

## 第八步：端到端验证

```bash
# 1. 技能加载
python -c "from core.skill_registry import SkillRegistry; r = SkillRegistry(); r.scan(); print(f'{len(r.skills)} skills loaded')"

# 2. 工具注册
python -c "from core.agent_loop import AgentLoop; a = AgentLoop(); print(f'{len(a.tool_registry.get_tool_schemas())} tools registered')"

# 3. Schema 格式检查
python -c "
from core.agent_loop import AgentLoop
a = AgentLoop()
schemas = a.tool_registry.get_tool_schemas()
for s in schemas:
    assert s['type'] == 'function'
    assert s['function']['strict'] == True
    assert s['function']['parameters']['additionalProperties'] == False
print('OK: 所有 Schema 符合 OpenAI Function Calling 格式')
"

# 4. CloudEvents 格式检查
python -c "
import asyncio
from core.event_bus import EventBus

async def test():
    bus = EventBus()
    received = []
    async def handler(event):
        received.append(event)
    bus.subscribe('test.cloudevents', handler)
    await bus.publish('test.cloudevents', {'msg': 'hello'}, source='test')
    await asyncio.sleep(0.1)
    e = received[0]
    assert e['specversion'] == '1.0'
    assert 'id' in e
    assert e['source'] == 'test'
    assert e['type'] == 'test.cloudevents'
    assert 'time' in e
    assert e['data']['msg'] == 'hello'
    print('OK: CloudEvents 格式验证通过')

asyncio.run(test())
"

# 5. JSON-RPC 2.0 格式检查
python -c "
import asyncio
from core.function_calling import ToolRegistry

async def test():
    t = ToolRegistry()
    result = await t.execute({'function': {'name': 'nonexistent', 'arguments': '{}'}})
    assert result.get('jsonrpc') == '2.0'
    assert result.get('error', {}).get('code') == -32601
    print('OK: JSON-RPC 2.0 格式验证通过')

asyncio.run(test())
"

# 6. validate_skills.py
python scripts/validate_skills.py

# 7. 单元测试
pytest tests/ -v

# 8. Pre-commit
pre-commit run --all-files

# 9. CLI 端到端
python channels/cli.py --query "系统健康吗"
```

### 最终提交

```bash
git add -A
git commit -m "refactor: 对齐业内标准（MCP Tool + OpenAI Function Calling + CloudEvents + JSON-RPC 2.0）"
git push
```

---

## 改动汇总

| 文件 | 操作 | 对齐标准 |
|------|------|---------|
| `skills/health_monitor/skill.json` | 修改 | MCP Tool 格式 |
| `skills/error_knowledge/skill.json` | 修改 | MCP Tool 格式 |
| `core/agent_loop.py` | 修改 | 读 `tools` 而非 `methods` |
| `core/function_calling.py` | 修改 | OpenAI Function Calling + JSON-RPC 2.0 |
| `core/event_bus.py` | 修改 | CloudEvents |
| `skills/error_knowledge/implementation.py` | 修改 | CloudEvents 事件解析 |
| `skills/health_monitor/implementation.py` | 修改 | CloudEvents 事件发布 |
| `scripts/validate_skills.py` | 修改 | MCP Tool 格式校验 |
| `CLAUDE.md` | 修改 | 更新规则引用 |

## 格式对比

| 组件 | 改动前（私有） | 改动后（业内标准） |
|------|-------------|-----------------|
| 技能定义 | `methods` + `params` | `tools` + `inputSchema`（MCP） |
| 工具 Schema | 基础格式 | `strict: true` + `additionalProperties: false`（OpenAI） |
| 事件 | `{"event_type": "..."}` | `{"specversion": "1.0", "type": "..."}`（CloudEvents） |
| 错误 | `{"error": "..."}` | `{"jsonrpc": "2.0", "error": {"code": -32601}}`（JSON-RPC 2.0） |

---

**从第一步开始执行，每步验证通过后再继续。**
```

---

这份文件把私有格式全部替换为业内标准，8 步完成。交给 Claude Code 执行。