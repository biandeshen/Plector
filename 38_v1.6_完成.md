---
tags: [Plector, 版本发布, 里程碑]
type: milestone
created: 2026-04-08
---

# 记忆功能已完成

```
v1.0.0 → 核心引擎 + 2 技能
v1.1.0 → 6 技能 + MCP 协议
v1.2.0 → WebSocket + Dashboard
v1.3.0 → HTTP+SSE 传输
v1.4.0 → SKILL.md 规范
v1.5.0 → SQLite MCP Server
v1.6.0 → 记忆功能
```

### 打 tag

```bash
git tag -a v1.6.0 -m "Plector v1.6.0 - 记忆功能

新增：
- memory 技能（6 个工具：对话/偏好/知识）
- AgentLoop 自动保存对话历史
- AgentLoop 自动加载记忆到 System Prompt
- session_id 隔离 + 异常降级 + 上下文截断

总计：7 技能，31 工具，11 核心模块，3 MCP Server，2 渠道"

git push origin v1.6.0
```

### 更新 README

```bash
python scripts/generate_readme.py
git add README.md
git commit -m "docs: 自动生成 README.md（7 skills, 31 tools）"
git push
```

### 验证

```bash
git tag -l
```

**预期**：
```
v1.0.0
v1.1.0
v1.2.0
v1.3.0
v1.4.0
v1.5.0
v1.6.0
```

**要执行吗？**